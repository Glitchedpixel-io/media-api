# Capability inventory

Generated by `uv run capability-inventory`. Do not hand-edit — re-run the harness instead. The machine-readable form of this document is `docs/capability-inventory.json`.

This document answers one question per endpoint: **what can a front end responsibly build on it?** Everything above each `UI verdict` line is the evidence for it. Where a value is not known, it says `UNKNOWN` and the [Gaps](#gaps) section says what would settle it.

## Run

- **Source:** media-api @ app.openapi()
- **App version:** 1.5.4.dev0+gc0c689a81.d20260826
- **Phases run:** 1 (static surface), 2 (code annotation), 3 (data shape), 4 (timed probes), 5 (dead surface)
- **Phases skipped:** none
- **Endpoints:** 96
- **Database:** read-only Postgres, connection fingerprint 5ee609328cdc (supplied via CAPINV_DATABASE_URL; host, database and credentials not recorded)
- **Server:** PostgreSQL 17.9 (Debian 17.9-1.pgdg13+1) on x86_64-pc-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit
- **Baseline database round trip:** 26ms median for `SELECT 1`. This is the unit cost of a query issued once per row, and it is a property of where the harness ran relative to the database rather than of the API. An API co-located with its database would see a far smaller number for the same defect — so read a per-row cost below as evidence of *how many* queries an endpoint issues, and treat the absolute milliseconds as specific to this measurement setup.
- Example column values were withheld (`--include-example-values` not passed): distinct counts and fill rates are recorded, the underlying rows are not.
- Probed `http://127.0.0.1:8077` with 37 probe(s).

## Summary

| Endpoint | Auth | Paging | Measured p95 | Verdict | One-line judgement |
|---|---|---|---|---|---|
| `GET /api/assets/` | bearer | keyset | 14604ms | not safe | Not safe as a first-screen query: worst-case p95 is 14604ms, and the cause is the per-row lazy load, not the paging |
| `POST /api/assets/` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/by-scheme/{scheme_id}/{external_id}` | bearer | **none** | UNKNOWN | UNKNOWN | UNKNOWN — the read is not index-covered, but was not timed; see Gaps |
| `PATCH /api/assets/seen` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}` | bearer | **none** | 133ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 133ms) |
| `PATCH /api/assets/{asset_id}` | bearer | — | UNKNOWN | write | Write path that touches the filesystem as well as the database |
| `GET /api/assets/{asset_id}/accessories` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `GET /api/assets/{asset_id}/derived_assets` | bearer | **none** | UNKNOWN | safe | Safe to render directly: the collection is bounded in practice at 4 rows (assets.master_asset_id -> assets) |
| `PUT /api/assets/{asset_id}/derived_assets/{child_asset_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/ids` | bearer | **none** | 105ms | safe | Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 105ms |
| `POST /api/assets/{asset_id}/ids` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/assets/{asset_id}/ids/{record_id}` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/assets/{asset_id}/ids/{record_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/metadata` | bearer | **none** | 153ms | safe | Safe to render directly: the collection is bounded in practice at 1 rows (measured directly from a probe response), measured p95 153ms |
| `POST /api/assets/{asset_id}/metadata` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/assets/{asset_id}/metadata/{metadata_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/metadata/{metadata_id}` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `PATCH /api/assets/{asset_id}/metadata/{metadata_id}` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/assets/{asset_id}/streams` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/streams` | bearer | **none** | 190ms | safe | Safe to render directly: the collection is bounded in practice at 3 rows (measured directly from a probe response), measured p95 190ms |
| `POST /api/assets/{asset_id}/streams` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/tags` | bearer | **none** | 134ms | safe | Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 134ms |
| `POST /api/assets/{asset_id}/tags` | bearer | — | UNKNOWN | write | Write path |
| `PUT /api/assets/{asset_id}/tags` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/assets/{asset_id}/tags/{tag_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/assets/{asset_id}/titles` | bearer | **none** | 144ms | safe | Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 144ms |
| `GET /api/assets/{asset_id}/transform_requests` | bearer | **none** | UNKNOWN | safe | Safe to render directly: the collection is bounded in practice at 17 rows (media_transform_requests.asset_id -> assets) |
| `POST /api/assets/{asset_id}/transform_requests` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/external-ids/resolve` | bearer | **none** | UNKNOWN | UNKNOWN | UNKNOWN — the read is not index-covered, but was not timed; see Gaps |
| `GET /api/fetch/{asset_id}` | bearer | **none** | UNKNOWN | not safe | UNKNOWN — streaming behaviour was not measured |
| `GET /api/health` | **none** | **none** | 141ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 141ms) |
| `GET /api/id_schemes` | bearer | **none** | 115ms | safe | Safe to render directly: the collection is bounded in practice at 3 rows (measured directly from a probe response), measured p95 115ms |
| `POST /api/id_schemes` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/id_schemes/{scheme_id}` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `PATCH /api/id_schemes/{scheme_id}` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/inbox` | bearer | — | UNKNOWN | write | Write path that touches the filesystem as well as the database |
| `GET /api/inbox` | bearer | **none** | UNKNOWN | UNKNOWN | UNKNOWN — this endpoint returns an entire collection with no cap, and the largest collection in the data was not measured |
| `POST /api/inbox` | bearer | — | UNKNOWN | write | Write path that touches the filesystem as well as the database |
| `POST /api/jobs` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/jobs/{job_key}/completed` | bearer | — | UNKNOWN | write | Write path |
| `PUT /api/jobs/{job_key}/heartbeat` | bearer | — | UNKNOWN | write | Write path |
| `POST /api/log` | **none** | — | UNKNOWN | write | Write path |
| `GET /api/ping` | **none** | **none** | 2ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 2ms) |
| `POST /api/run_summaries` | bearer | — | UNKNOWN | write | Write path |
| `POST /api/runner_state` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/runner_state/{runner_key}` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `PATCH /api/runner_state/{runner_key}` | bearer | — | UNKNOWN | write | Write path |
| `POST /api/scanner_run_summaries` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/search/transcripts` | bearer | offset | 3ms | caution | Safe for a search-results panel showing the first few pages (worst-case p95 3ms), and not safe for anything that scrolls indefinitely: the offset… |
| `GET /api/streams` | bearer | **none** | 1856ms | not safe | Not safe to render directly: the largest collection is 65,739 rows (measured directly from a probe response), measured p95 1856ms, returned in a… |
| `GET /api/streams/{stream_id}` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `PATCH /api/streams/{stream_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/tags` | bearer | keyset | 120ms | caution | Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500 |
| `POST /api/tags` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/tags/{tag_id}` | bearer | **none** | 103ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 103ms) |
| `PATCH /api/tags/{tag_id}` | bearer | — | UNKNOWN | write | Write path |
| `PUT /api/tags/{tag_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/tags/{tag_id}/tags` | bearer | keyset | UNKNOWN | UNKNOWN | UNKNOWN — cursor pagination means deep pages do not degrade the way offset does, but no timings were taken, so first-screen cost is unmeasured; see… |
| `POST /api/tags/{tag_id}/tags` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/title_types` | bearer | **none** | UNKNOWN | safe | Safe to render directly: the collection is bounded in practice at 8 rows (all 8 rows of `title_types`) |
| `POST /api/title_types` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/title_types/{title_type_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/title_types/{title_type_id}` | bearer | **none** | UNKNOWN | UNKNOWN | Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps |
| `PATCH /api/title_types/{title_type_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/` | bearer | keyset | 118ms | caution | Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500 |
| `POST /api/titles/` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/by-scheme/{scheme_id}/{external_id}` | bearer | **none** | UNKNOWN | UNKNOWN | UNKNOWN — the read is not index-covered, but was not timed; see Gaps |
| `GET /api/titles/{parent_title_id}/contents` | bearer | **none** | 186ms | safe | Safe to render directly: the collection is bounded in practice at 1 rows (measured directly from a probe response), measured p95 186ms |
| `POST /api/titles/{parent_title_id}/contents` | bearer | — | UNKNOWN | write | Write path |
| `POST /api/titles/{parent_title_id}/contents/positioned` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/titles/{parent_title_id}/contents/{title_contents_id}` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/titles/{parent_title_id}/contents/{title_contents_id}` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/titles/{parent_title_id}/contents/{title_contents_id}/reorder` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/{title_id}` | bearer | **none** | 110ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 110ms) |
| `PATCH /api/titles/{title_id}` | bearer | — | UNKNOWN | write | Write path |
| `PUT /api/titles/{title_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/{title_id}/ids` | bearer | **none** | UNKNOWN | not safe | Not safe to render directly: the largest collection is 701 rows (external_identifiers.scheme_id -> id_schemes), returned in a single uncapped response |
| `POST /api/titles/{title_id}/ids` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/titles/{title_id}/ids/{record_id}` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/titles/{title_id}/ids/{record_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/{title_id}/references` | bearer | **none** | UNKNOWN | not safe | Not safe to render directly: the largest collection is 727 rows (titles.title_type_id -> title_types), returned in a single uncapped response |
| `POST /api/titles/{title_id}/references` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/titles/{title_id}/references/{reference_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/titles/{title_id}/tags` | bearer | **none** | 141ms | safe | Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 141ms |
| `POST /api/titles/{title_id}/tags` | bearer | — | UNKNOWN | write | Write path |
| `PUT /api/titles/{title_id}/tags` | bearer | — | UNKNOWN | write | Write path |
| `DELETE /api/titles/{title_id}/tags/{tag_id}` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/transform_requests` | bearer | keyset | 153ms | caution | Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500 |
| `POST /api/transform_requests/claim` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/transform_requests/{request_id}` | bearer | **none** | 111ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 111ms) |
| `PATCH /api/transform_requests/{request_id}` | bearer | — | UNKNOWN | write | Write path |
| `PATCH /api/transform_requests/{request_id}/heartbeat` | bearer | — | UNKNOWN | write | Write path |
| `POST /api/transform_requests/{request_id}/link` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/transform_requests/{request_id}/logs` | bearer | **none** | UNKNOWN | safe | Safe to render directly: the collection is bounded in practice at 17 rows (media_transform_requests.asset_id -> assets) |
| `PATCH /api/transform_requests/{request_id}/retry` | bearer | — | UNKNOWN | write | Write path |
| `GET /api/version` | **none** | **none** | 8ms | safe | Safe for a detail view and fast enough to prefetch on hover (p95 8ms) |

## Endpoints

### GET /api/assets/

**Purpose:** Read Assets
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.read_assets`
**Pagination:** keyset, default 50, cap 500, ordering stable under concurrent writes (cursor is a keyset bookmark and normalize_sort() (app/utils/sorting.py) always appends `id` as a final tie-breaker, so ordering is total and a concurrent insert cannot shift rows across an already-issued page boundary)
**Trailing slash:** required — the same path without it returns a 307 redirect

**Response 200:** `PaginatedResponse_AssetReadExtended_` (rows are `AssetReadExtended`)
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`, `master_asset?: AssetRead | None` *(only with `include=master_asset`)*, `tags?: list[TagRead] | None` *(only with `include=tags`)*, `external_ids?: list[ExternalIdentifierRead] | None`
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `after` | query | str \| None | n/a | — |
| `before` | query | str \| None | n/a | — |
| `created_since` | query | str(date-time) \| None | **no** | no index on assets.created_at; requires a sequential scan |
| `duration_max` | query | float \| None (minimum=0) | **no** | no index on assets.duration; requires a sequential scan |
| `duration_min` | query | float \| None (minimum=0) | **no** | no index on assets.duration; requires a sequential scan |
| `filename_ext` | query | str \| None | **no** | suffix match on assets.filename; the pattern has a leading wildcard, which a btree cannot use at all, so this is a sequential scan regardless of indexing (a trigram index would be needed) |
| `include` | query | str \| None | n/a | — |
| `limit` | query | int (maximum=500, minimum=1) | n/a | — |
| `path_part` | query | str \| None | **no** | substring match on assets.path; the pattern has a leading wildcard, which a btree cannot use at all, so this is a sequential scan regardless of indexing (a trigram index would be needed) |
| `path_prefix` | query | str \| None | **no** | assets_path_key exists on assets.path but the match is case-insensitive (ILIKE) and the index uses the default case-sensitive opclass, so the planner cannot use it; a lower(path) expression index or text_pattern_ops would |
| `size_max` | query | int \| None (minimum=0) | **no** | no index on assets.size; requires a sequential scan |
| `size_min` | query | int \| None (minimum=0) | **no** | no index on assets.size; requires a sequential scan |
| `sort` | query | str | n/a | — |
| `tag_ids` | query | str \| None | yes (`ix_asset_tags_tag_id`) | served by ix_asset_tags_tag_id |
| `sort=created_at` | sort | — | **no** | no index on assets.created_at; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=duration` | sort | — | **no** | no index on assets.duration; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=filename` | sort | — | **no** | no index on assets.filename; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=id` | sort | — | yes (`assets_pkey`) | served by assets_pkey |
| `sort=mtime` | sort | — | **no** | no index on assets.mtime; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=path` | sort | — | yes (`assets_path_key`) | served by assets_path_key |
| `sort=size` | sort | — | **no** | no index on assets.size; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |

**Queries issued:**
- `select_page` on `assets`, `asset_tags` in `SQLAlchemyMediaRepository.list_paged` (app/repositories/media_repository.py:118)
- `select` on `external_identifiers` in `AssetORM.external_ids (ORM lazy load)` (app/models/ (relationship strategy)) **[N+1]** — lazy='select' relationship AssetORM.external_ids is serialised by the response model; SQLAlchemy emits one SELECT on external_identifiers per row returned unless the caller passes include= to eager-load it

**Limits:** `limit` <= 500; `limit` >= 1

**Data shape:** `asset_tags` 2,743 rows · `assets` 13,321 rows · `external_identifiers` 1,490 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled · fill rate not reported for `master_asset`, `tags` (returned empty unless `include=` asks for them)
**Collection sizes:** `asset_tags` per `assets` — min 1 / median 2 / p95 3 / max 3 (1,359 of 13,321 parents have any) · `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any) · `asset_tags` per `tags` — min 1 / median 23 / p95 496 / max 725 (35 of 43 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `assets-page-1` (`/api/assets/?limit=50`): p50 1566ms · p95 1641ms · 19.0KB · 50 items · n=7 — default first page a browse screen would issue
- `assets-page-1-with-includes` (`/api/assets/?include=tags,master_asset,external_ids&limit=50`): p50 172ms · p95 178ms · 19.0KB · 50 items · n=7 — same page with every optional relation eager-loaded
- `assets-scaling-10-rows` (`/api/assets/?limit=10`): p50 430ms · p95 452ms · 3.9KB · 10 items · n=7 — N+1 scaling, 10 rows
- `assets-scaling-200-rows` (`/api/assets/?limit=200`): p50 5954ms · p95 6020ms · 78.8KB · 200 items · n=7 — N+1 scaling, 200 rows -- compare against the 10- and 50-row probes
- `assets-max-page-unindexed-sort` (`/api/assets/?limit=500&sort=size:desc`): p50 14491ms · p95 14604ms · 199.0KB · 500 items · n=7 — largest permitted page on an unindexed sort key, N+1 left in place
- `assets-max-page-unindexed-sort-no-n1` (`/api/assets/?include=external_ids&limit=500&sort=size:desc`): p50 249ms · p95 263ms · 199.0KB · 500 items · n=7 — the same worst case with the per-row lazy load eliminated. The gap between this and the probe above is the N+1; what remains is the sort itself
- `assets-indexed-sort-no-n1` (`/api/assets/?include=external_ids&limit=500&sort=id:asc`): p50 248ms · p95 281ms · 197.6KB · 500 items · n=7 — control for the probe above -- an indexed sort key with the N+1 removed, so the two differ only in whether the sort column is indexed
- `assets-broadest-filter` (`/api/assets/?limit=50&path_part=e`): p50 1616ms · p95 1683ms · 19.0KB · 50 items · n=7 — substring filter matching almost everything; sequential scan by construction
- `assets-deep-page` (`/api/assets/?after=>i:5756&limit=50`): p50 1616ms · p95 1697ms · 17.9KB · 50 items · n=7 — page 41 reached by following cursors; compare against assets-page-1
  - deep page reached by following 40 `page.next` cursors; a keyset endpoint offers no way to jump straight to it
**Risk:**
- one extra SELECT per row: AssetORM.external_ids (ORM lazy load) is `lazy='select'` and is serialised into the response, so a page of 500 rows costs up to 500 additional queries against `external_identifiers`; at the 26ms round trip measured against the probed database that is about 13s of pure latency at the cap
- unindexed sort keys (`created_at`, `duration`, `filename`, `mtime`, `size`); every page sorts the whole filtered set, but `assets` holds only 13,321 rows, so this is latent rather than live -- the measured cost of an unindexed sort is currently indistinguishable from an indexed one. It becomes real as the table grows
- filters that cannot use an index (`created_since`, `duration_max`, `duration_min`, `filename_ext`, `path_part`, `path_prefix`, `size_max`, `size_min`); each forces a sequential scan, though `assets` holds only 13,321 rows, so the scan is currently cheap. This is a constraint on how large the table can grow before search becomes the bottleneck, not a live cost
- measured p95 of 14604ms on `assets-max-page-unindexed-sort` is a page-load-scale wait
- the trailing slash is required; requesting it without one gets a 307, which a cross-origin fetch with credentials will not always follow
- fields that are empty unless requested, and indistinguishable from genuinely empty: `master_asset`, `tags`
**UI verdict:** Not safe as a first-screen query: worst-case p95 is 14604ms, and the cause is the per-row lazy load, not the paging. Cursor paging itself holds up — page 400 costs what page 1 does — so infinite scroll is sound once the per-page cost is fixed.

### POST /api/assets/

**Purpose:** Create Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.create_asset`
**Pagination:** none — single object; not a collection
**Trailing slash:** required — the same path without it returns a 307 redirect

**Response 201:** `AssetRead`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | AssetCreatePublic | n/a | — |

**Queries issued:**
- `add` on `assets` in `SQLAlchemyMediaRepository.create` (app/repositories/media_repository.py:32)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `assets` in `SQLAlchemyMediaRepository.create` (app/repositories/media_repository.py:34)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- the trailing slash is required; requesting it without one gets a 307, which a cross-origin fetch with credentials will not always follow
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/by-scheme/{scheme_id}/{external_id}

**Purpose:** Get Asset By External Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.get_asset_by_external_id`
**Pagination:** none — single object; not a collection

**Response 200:** `AssetRead`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `404` No asset with that scheme and external ID found, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `external_id` | path | str | n/a | — |
| `scheme_id` | path | int | n/a | — |
| `assets.external_ids` | lookup | — | **no** | any on assets.external_ids cannot use a plain btree index |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get_by_external_id` (app/repositories/media_repository.py:48)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `assets.external_ids`; the read is a sequential scan
**UI verdict:** UNKNOWN — the read is not index-covered, but was not timed; see Gaps.

### PATCH /api/assets/seen

**Purpose:** Mark Assets Seen
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.mark_assets_seen`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | AssetSeenBatch | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |

**Queries issued:**
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `assets` in `SQLAlchemyMediaRepository.mark_assets_seen` (app/repositories/media_repository.py:153)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}

**Purpose:** Read Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.read_asset`
**Pagination:** none — single object; not a collection

**Response 200:** `AssetReadExtended`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`, `master_asset?: AssetRead | None` *(only with `include=master_asset`)*, `tags?: list[TagRead] | None` *(only with `include=tags`)*, `external_ids?: list[ExternalIdentifierRead] | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)
- `select` on `external_identifiers` in `AssetORM.external_ids (ORM lazy load)` (app/models/ (relationship strategy))

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `external_identifiers` 1,490 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled · fill rate not reported for `master_asset`, `tags` (returned empty unless `include=` asks for them)
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-detail` (`/api/assets/1065`): p50 128ms · p95 133ms · 358B · n=7
**Risk:**
- fields that are empty unless requested, and indistinguishable from genuinely empty: `master_asset`, `tags`
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 133ms).

### PATCH /api/assets/{asset_id}

**Purpose:** Update Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.update_asset`
**Pagination:** none — single object; not a collection

**Response 200:** `AssetRead`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | AssetPatchPublic | n/a | — |
| `asset_id` | path | int | n/a | — |
| `perform_rename` | query | bool | n/a | — |
| `assets.id` | lookup | — | UNKNOWN | unrecognised operator '!=' |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |
| `assets.path` | lookup | — | yes (`assets_path_key`) | served by assets_path_key |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)
- `execute` on `assets` in `SQLAlchemyMediaRepository.path_exists` (app/repositories/media_repository.py:58)
- `execute` on `assets` in `SQLAlchemyMediaRepository.update` (app/repositories/media_repository.py:133)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyMediaRepository.update` (app/repositories/media_repository.py:144)

**Filesystem:** `Path(media_root_str).resolve()`, `os.path.exists()`, `os.rename()`
**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path that touches the filesystem as well as the database. Not safe for optimistic UI: it can fail after the database row exists, so the UI must wait for the response before showing success.

### GET /api/assets/{asset_id}/accessories

**Purpose:** List Accessories
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.files.list_accessories`
**Pagination:** none — single object; not a collection

**Response 200:** `AccessoryFilePage` (rows are `AccessoryFile`)
Fields: `filename: str`, `size: int`, `mtime: float`
Also declares: `400` Invalid accessory path, `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)

**Filesystem:** `Path(config.accessory_root).resolve()`, `acc_dir.exists()`, `acc_dir.is_dir()`, `entry.is_file()`, `entry.stat()`, `os.scandir()`
**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `mtime` 99% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### GET /api/assets/{asset_id}/derived_assets

**Purpose:** Read Derived Assets
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.relationships.read_derived_assets`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[AssetRead]` (rows are `AssetRead`)
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |
| `assets.master_asset_id` | lookup | — | yes (`ix_assets_master_asset_id`) | served by ix_assets_master_asset_id |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)
- `execute` on `assets` in `SQLAlchemyMediaRepository.list_derived_assets` (app/repositories/media_repository.py:61)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no pagination, but the largest collection measured is only 4 rows (assets.master_asset_id -> assets), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 4 rows (assets.master_asset_id -> assets). The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### PUT /api/assets/{asset_id}/derived_assets/{child_asset_id}

**Purpose:** Declare Derived Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.relationships.declare_derived_asset`
**Pagination:** none — single object; not a collection

**Response 200:** `AssetRead`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `child_asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.update` (app/repositories/media_repository.py:133)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyMediaRepository.update` (app/repositories/media_repository.py:144)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `master_asset_id` 0% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/ids

**Purpose:** Get Asset Ids
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.external_ids.get_asset_ids`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[ExternalIdentifierReadExtended]` (rows are `ExternalIdentifierReadExtended`)
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`, `scheme?: IdSchemeRead | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |

**Queries issued:**
- `execute` on `id_schemes`, `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.list_for_entity` (app/repositories/external_identifier_repository.py:84) **[N+1]** — comprehension
- `select` on `id_schemes` in `ExternalIdentifierORM.scheme (ORM lazy load)` (app/models/ (relationship strategy)) **[N+1]** — lazy='select' relationship ExternalIdentifierORM.scheme is serialised by the response model; SQLAlchemy emits one SELECT on id_schemes per row returned

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `external_identifiers` 1,490 rows · `id_schemes` 3 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`) · `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-external-ids` (`/api/assets/1065/ids`): p50 94ms · p95 105ms · 2B · 0 items · n=7
**Risk:**
- one extra SELECT per row: ExternalIdentifierORM.scheme (ORM lazy load) is `lazy='select'` and is serialised into the response, so a page of N rows costs up to N additional queries against `id_schemes`
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyExternalIdentifierRepository.list_for_entity (comprehension)
- no pagination, but the largest collection measured is only 0 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 105ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/assets/{asset_id}/ids

**Purpose:** Create Asset Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.external_ids.create_asset_id`
**Pagination:** none — single object; not a collection

**Response 201:** `ExternalIdentifierRead`
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`
Also declares: `404` Asset not found, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | ExternalIdentifierCreatePublic | n/a | — |
| `asset_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)
- `add` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.create` (app/repositories/external_identifier_repository.py:90)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.create` (app/repositories/external_identifier_repository.py:92)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `external_identifiers` 1,490 rows · `titles` 1,573 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/assets/{asset_id}/ids/{record_id}

**Purpose:** Delete Asset Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.external_ids.delete_asset_id`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` External ID not found or doesn't belong to this asset, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `record_id` | path | int | n/a | — |
| `external_identifiers.id` | lookup | — | yes (`external_identifiers_pkey`) | served by external_identifiers_pkey |

**Queries issued:**
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.get` (app/repositories/external_identifier_repository.py:66)
- `execute` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.delete` (app/repositories/external_identifier_repository.py:114)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)

**Limits:** none declared

**Data shape:** `external_identifiers` 1,490 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/assets/{asset_id}/ids/{record_id}

**Purpose:** Update Asset Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.external_ids.update_asset_id`
**Pagination:** none — single object; not a collection

**Response 200:** `ExternalIdentifierRead`
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`
Also declares: `404` External ID not found or doesn't belong to this asset, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | ExternalIdentifierPatchPublic | n/a | — |
| `asset_id` | path | int | n/a | — |
| `record_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.get` (app/repositories/external_identifier_repository.py:66)
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.update` (app/repositories/external_identifier_repository.py:99)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.update` (app/repositories/external_identifier_repository.py:108)

**Limits:** none declared

**Data shape:** `external_identifiers` 1,490 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/metadata

**Purpose:** Get Asset Metadata
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.metadata.get_asset_metadata`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[MetadataRead]` (rows are `MetadataRead`)
Fields: `id: int`, `metadata_type: str`, `data: object`, `asset_id: int`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `metadata.asset_id` | lookup | — | **no** | no index on metadata.asset_id; requires a sequential scan |

**Queries issued:**
- `execute` on `metadata` in `SQLAlchemyMetadataRepository.get_asset_metadata` (app/repositories/metadata_repository.py:26)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `metadata` 15,050 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `metadata` per `assets` — min 1 / median 1 / p95 2 / max 3 (13,286 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `metadata.metadata_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-metadata` (`/api/assets/1065/metadata`): p50 132ms · p95 153ms · 2.9KB · 1 items · n=7
**Risk:**
- unindexed lookup on `metadata.asset_id`; the read is a sequential scan
- no pagination, but the largest collection measured is only 1 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 1 rows (measured directly from a probe response), measured p95 153ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/assets/{asset_id}/metadata

**Purpose:** Create Asset Metadata
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.metadata.create_asset_metadata`
**Pagination:** none — single object; not a collection

**Response 201:** `MetadataRead`
Fields: `id: int`, `metadata_type: str`, `data: object`, `asset_id: int`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | MetadataCreatePublic | n/a | — |
| `asset_id` | path | int | n/a | — |

**Queries issued:**
- `add` on `metadata` in `SQLAlchemyMetadataRepository.create` (app/repositories/metadata_repository.py:31)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `metadata` in `SQLAlchemyMetadataRepository.create` (app/repositories/metadata_repository.py:33)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `metadata` 15,050 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `metadata` per `assets` — min 1 / median 1 / p95 2 / max 3 (13,286 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `metadata.metadata_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/assets/{asset_id}/metadata/{metadata_id}

**Purpose:** Delete Asset Metadata
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.metadata.delete_asset_metadata`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `metadata_id` | path | int | n/a | — |
| `metadata.id` | lookup | — | yes (`metadata_pkey`) | served by metadata_pkey |

**Queries issued:**
- `get` on `metadata` in `SQLAlchemyMetadataRepository.get` (app/repositories/metadata_repository.py:37)
- `execute` on `metadata` in `SQLAlchemyMetadataRepository.delete` (app/repositories/metadata_repository.py:57)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `metadata` 15,050 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `metadata` per `assets` — min 1 / median 1 / p95 2 / max 3 (13,286 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `metadata.metadata_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/metadata/{metadata_id}

**Purpose:** Get Asset Metadata Item
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.metadata.get_asset_metadata_item`
**Pagination:** none — single object; not a collection

**Response 200:** `MetadataRead`
Fields: `id: int`, `metadata_type: str`, `data: object`, `asset_id: int`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `metadata_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `metadata` in `SQLAlchemyMetadataRepository.get` (app/repositories/metadata_repository.py:37)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `metadata` 15,050 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `metadata` per `assets` — min 1 / median 1 / p95 2 / max 3 (13,286 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `metadata.metadata_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### PATCH /api/assets/{asset_id}/metadata/{metadata_id}

**Purpose:** Update Asset Metadata
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.metadata.update_asset_metadata`
**Pagination:** none — single object; not a collection

**Response 200:** `MetadataRead`
Fields: `id: int`, `metadata_type: str`, `data: object`, `asset_id: int`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | MetadataPatchPublic | n/a | — |
| `asset_id` | path | int | n/a | — |
| `metadata_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `metadata` in `SQLAlchemyMetadataRepository.get` (app/repositories/metadata_repository.py:37)
- `get` on `metadata` in `SQLAlchemyMetadataRepository.update` (app/repositories/metadata_repository.py:41)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `metadata` in `SQLAlchemyMetadataRepository.update` (app/repositories/metadata_repository.py:52)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `metadata` 15,050 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `metadata` per `assets` — min 1 / median 1 / p95 2 / max 3 (13,286 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `metadata.metadata_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/assets/{asset_id}/streams

**Purpose:** Delete Asset Streams
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.streams.delete_asset_streams`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `streams.asset_id` | lookup | — | **no** | no index on streams.asset_id; requires a sequential scan |

**Queries issued:**
- `execute` on `streams` in `SQLAlchemyStreamRepository.delete_asset_streams` (app/repositories/stream_repository.py:52)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `streams` 65,745 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `streams.asset_id`; the read is a sequential scan
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/streams

**Purpose:** Get Asset Streams
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.streams.get_asset_streams`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[StreamRead]` (rows are `StreamRead`)
Fields: `id: int`, `stream_index?: int | None`, `codec_type: str`, `codec_name?: str | None`, `language?: str | None`, `width?: int | None`, `height?: int | None`, `frame_rate?: float | None`, `channels?: int | None`, `sample_rate?: int | None`, `is_default?: bool | None`, `is_forced?: bool | None`, `title?: str | None`, `asset_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |
| `streams.asset_id` | lookup | — | **no** | no index on streams.asset_id; requires a sequential scan |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)
- `execute` on `streams` in `SQLAlchemyStreamRepository.get_asset_streams` (app/repositories/stream_repository.py:47)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `streams` 65,745 rows · `title` 22% filled · `frame_rate` 29% filled · `height` 29% filled · `width` 29% filled · `channels` 32% filled · `sample_rate` 32% filled · `language` 92% filled · `codec_name` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-streams` (`/api/assets/1065/streams`): p50 168ms · p95 190ms · 699B · 3 items · n=7
**Risk:**
- unindexed lookup on `streams.asset_id`; the read is a sequential scan
- no pagination, but the largest collection measured is only 3 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 3 rows (measured directly from a probe response), measured p95 190ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/assets/{asset_id}/streams

**Purpose:** Create Asset Stream
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.streams.create_asset_stream`
**Pagination:** none — single object; not a collection

**Response 201:** `StreamRead`
Fields: `id: int`, `stream_index?: int | None`, `codec_type: str`, `codec_name?: str | None`, `language?: str | None`, `width?: int | None`, `height?: int | None`, `frame_rate?: float | None`, `channels?: int | None`, `sample_rate?: int | None`, `is_default?: bool | None`, `is_forced?: bool | None`, `title?: str | None`, `asset_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | StreamCreatePublic | n/a | — |
| `asset_id` | path | int | n/a | — |

**Queries issued:**
- `add` on `streams` in `SQLAlchemyStreamRepository.create` (app/repositories/stream_repository.py:15)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `streams` in `SQLAlchemyStreamRepository.create` (app/repositories/stream_repository.py:17)

**Limits:** none declared

**Data shape:** `streams` 65,745 rows · `title` 22% filled · `frame_rate` 29% filled · `height` 29% filled · `width` 29% filled · `channels` 32% filled · `sample_rate` 32% filled · `language` 92% filled · `codec_name` 100% filled
**Collection sizes:** `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/tags

**Purpose:** Get Asset Tags
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.tags.get_asset_tags`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TagRead]` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `asset_tags.asset_id` | lookup | — | yes (`ix_asset_tags_asset_id`) | served by ix_asset_tags_asset_id |

**Queries issued:**
- `execute` on `tags`, `asset_tags` in `SQLAlchemyTagRepository.get_asset_tags` (app/repositories/tag_repository.py:170) **[N+1]** — comprehension
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `asset_tags` 2,743 rows · `assets` 13,321 rows · `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `asset_tags` per `assets` — min 1 / median 2 / p95 3 / max 3 (1,359 of 13,321 parents have any) · `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `asset_tags` per `tags` — min 1 / median 23 / p95 496 / max 725 (35 of 43 parents have any) · `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-tags` (`/api/assets/1065/tags`): p50 127ms · p95 134ms · 2B · 0 items · n=7
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyTagRepository.get_asset_tags (comprehension)
- no pagination, but the largest collection measured is only 0 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 134ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/assets/{asset_id}/tags

**Purpose:** Tag Asset By Name
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.tags.tag_asset_by_name`
**Pagination:** none — single object; not a collection

**Response 200:** `TaggingReport`
Fields: `added_tags: list[TagRead]`, `tagging_errors: list[str]`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagNameSet | n/a | — |
| `asset_id` | path | int | n/a | — |
| `asset_tags.asset_id` | lookup | — | yes (`ix_asset_tags_asset_id`) | served by ix_asset_tags_asset_id |
| `tags.name` | lookup | — | yes (`ix_tags_name`) | served by ix_tags_name |

**Queries issued:**
- `execute` on `tags` in `SQLAlchemyTagRepository.get_by_name` (app/repositories/tag_repository.py:56) **[N+1]** — comprehension
- `add` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:41) **[N+1]** — for tag_name in unique_names
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35) **[N+1]** — for tag_name in unique_names
- `refresh` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:43) **[N+1]** — for tag_name in unique_names
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47) **[N+1]** — for tag_name in unique_names
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `tags` in `SQLAlchemyTagRepository.get` (app/repositories/tag_repository.py:50) **[N+1]** — for tag_id in tag_ids
- `execute` on `asset_tags` in `SQLAlchemyTagRepository.add_asset_tags` (app/repositories/tag_repository.py:130)
- `add` on `asset_tags` in `SQLAlchemyTagRepository.add_asset_tags` (app/repositories/tag_repository.py:138) **[N+1]** — for tag_id in tag_ids
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `asset_tags` 2,743 rows · `assets` 13,321 rows · `tags` 43 rows
**Collection sizes:** `asset_tags` per `assets` — min 1 / median 2 / p95 3 / max 3 (1,359 of 13,321 parents have any) · `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `asset_tags` per `tags` — min 1 / median 23 / p95 496 / max 725 (35 of 43 parents have any) · `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyBaseRepository._safe_commit (for tag_name in unique_names); SQLAlchemyTagRepository.add_asset_tags (for tag_id in tag_ids); SQLAlchemyTagRepository.create (for tag_name in unique_names); SQLAlchemyTagRepository.exists (for tag_name in unique_names); SQLAlchemyTagRepository.get (for tag_id in tag_ids); SQLAlchemyTagRepository.get_by_name (comprehension)
**UI verdict:** Write path. Safe to call from a form submit, but the work is proportional to the size of the payload rather than constant — send small batches and show a determinate progress state rather than a spinner.

### PUT /api/assets/{asset_id}/tags

**Purpose:** Tag Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.tags.tag_asset`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TagRead]` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagSet | n/a | — |
| `asset_id` | path | int | n/a | — |
| `asset_tags.asset_id` | lookup | — | yes (`ix_asset_tags_asset_id`) | served by ix_asset_tags_asset_id |

**Queries issued:**
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `tags` in `SQLAlchemyTagRepository.get` (app/repositories/tag_repository.py:50) **[N+1]** — for tag_id in tag_ids
- `execute` on `asset_tags` in `SQLAlchemyTagRepository.add_asset_tags` (app/repositories/tag_repository.py:130)
- `add` on `asset_tags` in `SQLAlchemyTagRepository.add_asset_tags` (app/repositories/tag_repository.py:138) **[N+1]** — for tag_id in tag_ids
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** returns an unbounded list; the response grows with the number of related rows

**Data shape:** `asset_tags` 2,743 rows · `assets` 13,321 rows · `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `asset_tags` per `assets` — min 1 / median 2 / p95 3 / max 3 (1,359 of 13,321 parents have any) · `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `asset_tags` per `tags` — min 1 / median 23 / p95 496 / max 725 (35 of 43 parents have any) · `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyTagRepository.add_asset_tags (for tag_id in tag_ids); SQLAlchemyTagRepository.get (for tag_id in tag_ids)
- no pagination and no page-size cap; the largest collection measured is 725 rows (asset_tags.tag_id -> tags)
**UI verdict:** Write path. Safe to call from a form submit, but the work is proportional to the size of the payload rather than constant — send small batches and show a determinate progress state rather than a spinner.

### DELETE /api/assets/{asset_id}/tags/{tag_id}

**Purpose:** Untag Asset By Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.tags.untag_asset_by_id`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`
Also declares: `204` Asset exists, but did not have the tag, `404` Asset does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `tag_id` | path | int | n/a | — |
| `asset_tags.asset_id` | lookup | — | yes (`ix_asset_tags_asset_id`) | served by ix_asset_tags_asset_id |
| `asset_tags.tag_id` | lookup | — | yes (`ix_asset_tags_tag_id`) | served by ix_asset_tags_tag_id |

**Queries issued:**
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `asset_tags` in `SQLAlchemyTagRepository.remove_asset_tag` (app/repositories/tag_repository.py:151)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** none declared

**Data shape:** `asset_tags` 2,743 rows · `assets` 13,321 rows
**Collection sizes:** `asset_tags` per `assets` — min 1 / median 2 / p95 3 / max 3 (1,359 of 13,321 parents have any) · `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `asset_tags` per `tags` — min 1 / median 23 / p95 496 / max 725 (35 of 43 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/assets/{asset_id}/titles

**Purpose:** Get Asset Titles
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.core.get_asset_titles`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TitleContentReadParent]` (rows are `TitleContentReadParent`)
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`, `parent_title: TitleRead`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `title_contents.asset_id` | lookup | — | **no** | no index on title_contents.asset_id; requires a sequential scan |

**Queries issued:**
- `execute` on `titles`, `title_contents` in `SQLAlchemyTitleContentRepository.get_titles_with_asset` (app/repositories/title_content_repository.py:96)
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `title_contents` 2,135 rows · `titles` 1,573 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `asset-titles` (`/api/assets/1065/titles`): p50 125ms · p95 144ms · 2B · 0 items · n=7
**Risk:**
- unindexed lookup on `title_contents.asset_id`; the read is a sequential scan
- no pagination, but the largest collection measured is only 0 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 144ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### GET /api/assets/{asset_id}/transform_requests

**Purpose:** Get Asset Transform Requests
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.transform_requests.get_asset_transform_requests`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TransformRequestRead]` (rows are `TransformRequestRead`)
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |
| `media_transform_requests.asset_id` | lookup | — | yes (`uniq_pending_transform_per_asset_and_type`) | served by uniq_pending_transform_per_asset_and_type |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)
- `execute` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.get_asset_transform_requests` (app/repositories/transform_request_repository.py:109)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no pagination, but the largest collection measured is only 17 rows (media_transform_requests.asset_id -> assets), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 17 rows (media_transform_requests.asset_id -> assets). The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/assets/{asset_id}/transform_requests

**Purpose:** Create Asset Transform Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.assets.transform_requests.create_asset_transform_request`
**Pagination:** none — single object; not a collection

**Response 201:** `TransformRequestRead`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TransformRequestCreatePublic | n/a | — |
| `asset_id` | path | int | n/a | — |

**Queries issued:**
- `add` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:27)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:29)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/external-ids/resolve

**Purpose:** Resolve External Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.external_ids.resolve_external_id`
**Pagination:** none — single object; not a collection

**Response 200:** `ExternalIdResolution`
Fields: `entity_type: EntityTypeEnum`, `entity_id: int`, `scheme_code: str`, `external_id: str`
Also declares: `404` External ID not found, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `external_id` | query | str | n/a | External ID value |
| `scheme` | query | str | n/a | Scheme code (e.g., 'imdb', 'tmdb') |
| `external_identifiers.external_id` | lookup | — | **no** | no index on external_identifiers.external_id; requires a sequential scan |
| `id_schemes.code` | lookup | — | yes (`id_schemes_code_key`) | served by id_schemes_code_key |

**Queries issued:**
- `execute` on `id_schemes`, `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.resolve_by_code` (app/repositories/external_identifier_repository.py:61)

**Limits:** none declared

**Data shape:** `external_identifiers` 1,490 rows · `id_schemes` 3 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`) · `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `external_identifiers.external_id`; the read is a sequential scan
**UI verdict:** UNKNOWN — the read is not index-covered, but was not timed; see Gaps.

### GET /api/fetch/{asset_id}

**Purpose:** Stream Asset
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.file_stream.stream_asset`
**Pagination:** none — byte-range streamed, not paged
**Streaming:** yes — the body is produced incrementally, not serialised

**Response 200:** `no model declared`
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `Range` | header | str \| None | n/a | — |
| `asset_id` | path | int | n/a | — |
| `assets.id` | lookup | — | yes (`assets_pkey`) | served by assets_pkey |

**Queries issued:**
- `execute` on `assets` in `SQLAlchemyMediaRepository.get` (app/repositories/media_repository.py:41)

**Filesystem:** `(media_root / rel).resolve()`, `Path(self.media_root).resolve()`, `abs_path.is_file()`, `abs_path.stat()`, `path.open()`
**Limits:** none declared

**Data shape:** `assets` 13,321 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `fetch-asset-stream` (`/api/fetch/1065`): ERROR — expected status [200], got 404 — time-to-first-byte measured separately from total transfer
  - Accept-Ranges header absent
- `fetch-asset-range` (`/api/fetch/1065`): ERROR — expected status [206], got 404 — first mebibyte only; verifies Content-Range and Accept-Ranges
  - Accept-Ranges header absent
- `fetch-asset-range-suffix` (`/api/fetch/1065`): ERROR — expected status [206], got 404 — suffix range, the form a seek-to-end scrub issues
  - Accept-Ranges header absent
- `fetch-asset-range-unsatisfiable` (`/api/fetch/1065`): ERROR — expected status [416], got 404 — verifies the unsatisfiable case returns 416 with a Content-Range header
  - Accept-Ranges header absent
**Risk:**
- the response size is bounded by the file on disk, not by anything the API declares; a client that issues no Range header will be sent the whole asset
- probe `fetch-asset-stream` failed: expected status [200], got 404
- probe `fetch-asset-range` failed: expected status [206], got 404
- probe `fetch-asset-range-suffix` failed: expected status [206], got 404
- probe `fetch-asset-range-unsatisfiable` failed: expected status [416], got 404
**UI verdict:** UNKNOWN — streaming behaviour was not measured. Range support is implemented in the service, but whether it works end to end against a real file has not been verified; see Gaps.

### GET /api/health

**Purpose:** Health Check
**Auth:** none (unauthenticated)
**Handler:** `app.app_factory.health_check`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`

**Queries issued:**
- `execute` on unresolved table in `health_check (inline)` (app/app_factory.py:242)

**Limits:** none declared

**Data shape:** no database tables are read by this endpoint.
**Measured:**
- `health` (`/api/health`): p50 113ms · p95 141ms · 236B · n=7 — unauthenticated; opens its own database and Elasticsearch connections
**Risk:**
- no authentication is required on this route
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 141ms).

### GET /api/id_schemes

**Purpose:** Get Id Schemes
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.id_schemes.get_id_schemes`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[IdSchemeRead]` (rows are `IdSchemeRead`)
Fields: `id: int`, `code: str`, `label: str`, `validator?: str | None`

**Queries issued:**
- `execute` on `id_schemes` in `SQLAlchemyIdSchemeRepository.list_all` (app/repositories/id_scheme_repository.py:37)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `id_schemes` 3 rows · `validator` 0% filled
**Facet candidates:** `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `id-schemes-all` (`/api/id_schemes`): p50 96ms · p95 115ms · 182B · 3 items · n=7
**Risk:**
- no pagination, but the largest collection measured is only 3 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 3 rows (measured directly from a probe response), measured p95 115ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/id_schemes

**Purpose:** Create Id Scheme
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.id_schemes.create_id_scheme`
**Pagination:** none — single object; not a collection

**Response 201:** `IdSchemeRead`
Fields: `id: int`, `code: str`, `label: str`, `validator?: str | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | IdSchemeCreatePublic | n/a | — |

**Queries issued:**
- `add` on `id_schemes` in `SQLAlchemyIdSchemeRepository.create` (app/repositories/id_scheme_repository.py:19)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `id_schemes` in `SQLAlchemyIdSchemeRepository.create` (app/repositories/id_scheme_repository.py:21)

**Limits:** none declared

**Data shape:** `id_schemes` 3 rows · `validator` 0% filled
**Facet candidates:** `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/id_schemes/{scheme_id}

**Purpose:** Get Id Scheme
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.id_schemes.get_id_scheme`
**Pagination:** none — single object; not a collection

**Response 200:** `IdSchemeRead`
Fields: `id: int`, `code: str`, `label: str`, `validator?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `scheme_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `id_schemes` in `SQLAlchemyIdSchemeRepository.get` (app/repositories/id_scheme_repository.py:25)

**Limits:** none declared

**Data shape:** `id_schemes` 3 rows · `validator` 0% filled
**Facet candidates:** `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### PATCH /api/id_schemes/{scheme_id}

**Purpose:** Update Scheme
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.id_schemes.update_scheme`
**Pagination:** none — single object; not a collection

**Response 200:** `IdSchemeRead`
Fields: `id: int`, `code: str`, `label: str`, `validator?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | IdSchemePatchPublic | n/a | — |
| `scheme_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `id_schemes` in `SQLAlchemyIdSchemeRepository.update` (app/repositories/id_scheme_repository.py:41)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `id_schemes` in `SQLAlchemyIdSchemeRepository.update` (app/repositories/id_scheme_repository.py:50)

**Limits:** none declared

**Data shape:** `id_schemes` 3 rows · `validator` 0% filled
**Facet candidates:** `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/inbox

**Purpose:** Delete From Inbox
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.inbox.delete_from_inbox`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `source` | query | str | n/a | — |

**Filesystem:** `(base / rel_norm).resolve()`, `(self.trash_root / rel).resolve()`, `src.exists()`
**Limits:** none declared

**Data shape:** no database tables are read by this endpoint.
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path that touches the filesystem as well as the database. Not safe for optimistic UI: it can fail after the database row exists, so the UI must wait for the response before showing success.

### GET /api/inbox

**Purpose:** List Inbox
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.inbox.list_inbox`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[InboxItem]` (rows are `InboxItem`)
Fields: `path: str`, `name: str`, `type: InboxItemTypeEnum`, `size?: int | None`, `children?: list[InboxItem] | None`

**Filesystem:** `p.is_file()`, `path.is_dir()`, `path.iterdir()`, `path.stat()`
**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** no database tables are read by this endpoint.
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no pagination and no page-size cap; the largest possible response is UNKNOWN without Phase 3
**UI verdict:** UNKNOWN — this endpoint returns an entire collection with no cap, and the largest collection in the data was not measured. Do not build a screen on it until Phase 3 has run; see Gaps.

### POST /api/inbox

**Purpose:** Import From Inbox
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.inbox.import_from_inbox`
**Pagination:** none — single object; not a collection

**Response 201:** `AssetRead`
Fields: `id: int`, `path: str`, `filename: str`, `duration: float`, `bitrate: int`, `container_format?: str | None`, `size: int`, `mtime?: str(date-time) | None`, `last_seen?: str(date-time) | None`, `master_asset_id?: int | None`, `created_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | InboxImportRequest | n/a | — |

**Queries issued:**
- `add` on `assets` in `SQLAlchemyMediaRepository.create` (app/repositories/media_repository.py:32)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `assets` in `SQLAlchemyMediaRepository.create` (app/repositories/media_repository.py:34)
- `add` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:27)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:29)

**Filesystem:** `(base / rel_norm).resolve()`, `abs_asset_path.stat()`, `dst.exists()`, `src.exists()`, `src.is_file()`
**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `media_transform_requests` 17,851 rows · `master_asset_id` 0% filled · `duration` 97% filled · `container_format` 99% filled · `mtime` 99% filled · `last_seen` 100% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path that touches the filesystem as well as the database. Not safe for optimistic UI: it can fail after the database row exists, so the UI must wait for the response before showing success.

### POST /api/jobs

**Purpose:** Create Job
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.jobs.create_job`
**Pagination:** none — single object; not a collection

**Response 201:** `JobRead`
Fields: `job_key: str`, `created_at: str(date-time)`, `heartbeat_at?: str(date-time) | None`, `completed_at?: str(date-time) | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `job_key` | query | str | n/a | — |

**Queries issued:**
- `add` on `jobs` in `SQLAlchemyJobRepository.create` (app/repositories/job_repository.py:28)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `jobs` in `SQLAlchemyJobRepository.create` (app/repositories/job_repository.py:30)

**Limits:** none declared

**Data shape:** `jobs` 3,011 rows · `heartbeat_at` 53% filled · `completed_at` 100% filled
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/jobs/{job_key}/completed

**Purpose:** Mark Completed
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.jobs.mark_completed`
**Pagination:** none — single object; not a collection

**Response 200:** `JobRead`
Fields: `job_key: str`, `created_at: str(date-time)`, `heartbeat_at?: str(date-time) | None`, `completed_at?: str(date-time) | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `job_key` | path | str | n/a | — |

**Queries issued:**
- `get` on `jobs` in `SQLAlchemyJobRepository.mark_complete` (app/repositories/job_repository.py:97)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `jobs` in `SQLAlchemyJobRepository.mark_complete` (app/repositories/job_repository.py:104)

**Limits:** none declared

**Data shape:** `jobs` 3,011 rows · `heartbeat_at` 53% filled · `completed_at` 100% filled
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PUT /api/jobs/{job_key}/heartbeat

**Purpose:** Mark Heartbeat
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.jobs.mark_heartbeat`
**Pagination:** none — single object; not a collection

**Response 200:** `JobRead`
Fields: `job_key: str`, `created_at: str(date-time)`, `heartbeat_at?: str(date-time) | None`, `completed_at?: str(date-time) | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `job_key` | path | str | n/a | — |

**Queries issued:**
- `get` on `jobs` in `SQLAlchemyJobRepository.heartbeat` (app/repositories/job_repository.py:74)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `jobs` in `SQLAlchemyJobRepository.heartbeat` (app/repositories/job_repository.py:81)

**Limits:** none declared

**Data shape:** `jobs` 3,011 rows · `heartbeat_at` 53% filled · `completed_at` 100% filled
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### POST /api/log

**Purpose:** Client Traces
**Auth:** none (unauthenticated)
**Handler:** `app.routers.logs.client_traces`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`

**External calls:** `httpx.AsyncClient()`
**Limits:** none declared

**Data shape:** no database tables are read by this endpoint.
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no authentication is required on this route
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/ping

**Purpose:** Ping
**Auth:** none (unauthenticated)
**Handler:** `app.app_factory.ping`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`

**Limits:** none declared

**Data shape:** no database tables are read by this endpoint.
**Measured:**
- `ping` (`/api/ping`): p50 1ms · p95 2ms · 15B · n=7
**Risk:**
- no authentication is required on this route
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 2ms).

### POST /api/run_summaries

**Purpose:** Create Run Summary
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.run_summaries.create_run_summary`
**Pagination:** none — single object; not a collection

**Response 201:** `RunSummaryRead`
Fields: `id: int`, `worker_name: str`, `worker_type: str`, `transform_type: str`, `started_at: str(date-time)`, `processed_count: int`, `success_count: int`, `failed_count: int`, `running_time: int`, `extras?: object | None`, `created_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | RunSummaryCreatePublic | n/a | — |

**Queries issued:**
- `add` on `run_summaries` in `SQLAlchemyRunSummaryRepository.create` (app/repositories/run_summary_repository.py:19)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `run_summaries` in `SQLAlchemyRunSummaryRepository.create` (app/repositories/run_summary_repository.py:21)

**Limits:** none declared

**Data shape:** `run_summaries` 948 rows
**Facet candidates:** `run_summaries.transform_type` — 6 distinct; values withheld (see `--include-example-values`) · `run_summaries.worker_type` — 6 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### POST /api/runner_state

**Purpose:** Create Runner State
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.runner_state.create_runner_state`
**Pagination:** none — single object; not a collection

**Response 201:** `RunnerStateRead`
Fields: `state?: object | None`, `runner_key: str`, `updated_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | RunnerStateCreatePublic | n/a | — |

**Queries issued:**
- `add` on `runner_state` in `SQLAlchemyRunnerStateRepository.create` (app/repositories/runner_state_repository.py:23)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `runner_state` in `SQLAlchemyRunnerStateRepository.create` (app/repositories/runner_state_repository.py:25)

**Limits:** none declared

**Data shape:** `runner_state` 5 rows
**Facet candidates:** `runner_state.runner_key` — 5 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/runner_state/{runner_key}

**Purpose:** Get Runner State
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.runner_state.get_runner_state`
**Pagination:** none — single object; not a collection

**Response 200:** `RunnerStateRead`
Fields: `state?: object | None`, `runner_key: str`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `runner_key` | path | str | n/a | — |

**Queries issued:**
- `get` on `runner_state` in `SQLAlchemyRunnerStateRepository.get_runner_state` (app/repositories/runner_state_repository.py:18)

**Limits:** none declared

**Data shape:** `runner_state` 5 rows
**Facet candidates:** `runner_state.runner_key` — 5 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### PATCH /api/runner_state/{runner_key}

**Purpose:** Partial Runner Update
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.runner_state.partial_runner_update`
**Pagination:** none — single object; not a collection

**Response 200:** `RunnerStateRead`
Fields: `state?: object | None`, `runner_key: str`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | RunnerStatePatchPublic | n/a | — |
| `runner_key` | path | str | n/a | — |
| `runner_state.runner_key` | lookup | — | yes (`runner_state_pkey`) | served by runner_state_pkey |

**Queries issued:**
- `execute` on `runner_state` in `SQLAlchemyRunnerStateRepository.set_runner_state` (app/repositories/runner_state_repository.py:32)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyRunnerStateRepository.set_runner_state` (app/repositories/runner_state_repository.py:43)

**Limits:** none declared

**Data shape:** `runner_state` 5 rows
**Facet candidates:** `runner_state.runner_key` — 5 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### POST /api/scanner_run_summaries

**Purpose:** Create Scanner Run Summary
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.scanner_run_summaries.create_scanner_run_summary`
**Pagination:** none — single object; not a collection

**Response 201:** `ScannerRunSummaryRead`
Fields: `id: int`, `worker_name: str`, `worker_type: str`, `scan_path?: str | None`, `relative_to_path?: str | None`, `started_at: str(date-time)`, `running_time: int`, `dry_run: bool`, `total_count?: int | None`, `processed_count: int`, `folder_count?: int | None`, `excluded_count?: int | None`, `previously_seen_count: int`, `error_count?: int | None`, `api_error_count?: int | None`, `no_metadata_count?: int | None`, `unsupported_file_count?: int | None`, `extras?: object | None`, `created_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | ScannerRunSummaryCreatePublic | n/a | — |

**Queries issued:**
- `add` on `scanner_run_summaries` in `SQLAlchemyScannerRunSummaryRepository.create` (app/repositories/run_summary_repository.py:37)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `scanner_run_summaries` in `SQLAlchemyScannerRunSummaryRepository.create` (app/repositories/run_summary_repository.py:39)

**Limits:** none declared

**Data shape:** `scanner_run_summaries` 54 rows · `api_error_count` 0% filled · `folder_count` 0% filled · `no_metadata_count` 0% filled · `relative_to_path` 0% filled · `scan_path` 0% filled · `total_count` 0% filled · `unsupported_file_count` 0% filled · `excluded_count` 13% filled
**Facet candidates:** `scanner_run_summaries.dry_run` — 2 distinct; values withheld (see `--include-example-values`) · `scanner_run_summaries.worker_name` — 13 distinct; values withheld (see `--include-example-values`) · `scanner_run_summaries.worker_type` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/search/transcripts

**Purpose:** Search Transcripts
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.search_transcripts.search_transcripts`
**Pagination:** offset, default 25, cap 200, **ordering NOT stable under concurrent writes** (Elasticsearch from/size over a relevance sort; a concurrent index write can shift scores and therefore row positions, so a row can be skipped or repeated across pages). Ceiling: from + size is bounded by the index's max_result_window (10,000 by default); beyond that the query is rejected outright rather than being slow

**Response 200:** `TranscriptSearchResponse` (rows are `TranscriptSearchHit`)
Fields: `asset_id: int`, `segment_id: int`, `start_s: float`, `end_s: float`, `media_path?: str | None`, `media_title?: str | None`, `language?: str | None`, `text?: str | None`, `score?: float | None`, `highlight?: list[str]`
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `asset_id` | query | int \| None | yes (`inverted index on asset_id`) | Elasticsearch `term` on `asset_id`; exact term lookup |
| `collection` | query | str \| None | yes (`inverted index on collection`) | Elasticsearch `term` on `collection`; exact term lookup |
| `language` | query | str \| None | yes (`inverted index on language`) | Elasticsearch `term` on `language`; exact term lookup |
| `mode` | query | str | n/a | — |
| `offset` | query | int (minimum=0) | n/a | — |
| `path_part` | query | str \| None | **no** | Elasticsearch `wildcard` on `media_path`; the pattern has a leading wildcard, so the term dictionary cannot be seeked and every term in the segment is scanned |
| `path_prefix` | query | str \| None | yes (`inverted index on media_path`) | Elasticsearch `prefix` on `media_path`; seekable in the term dictionary |
| `q` | query | str | n/a | — |
| `size` | query | int (maximum=200, minimum=1) | n/a | — |
| `title_part` | query | str \| None | **no** | Elasticsearch `wildcard` on `media_title.kw`; the pattern has a leading wildcard, so the term dictionary cannot be seeked and every term in the segment is scanned |

**External calls:** `Elasticsearch.search()`
**Limits:** `size` <= 200; `size` >= 1

**Data shape:** no database tables are read by this endpoint.
**Measured:**
- `search-transcripts-page-1` (`/api/search/transcripts?offset=0&q=the&size=25`): ERROR — expected status [200], got 503
- `search-transcripts-deep-page` (`/api/search/transcripts?offset=9975&q=the&size=25`): ERROR — expected status [200], got 503 — last page inside the default 10,000 max_result_window
  - offset=9975 requested directly
- `search-transcripts-past-window` (`/api/search/transcripts?offset=10000&q=the&size=25`): p50 2ms · p95 3ms · 54B · n=7 — deliberately past max_result_window; records how the failure surfaces
  - offset=10000 requested directly
  - responded 503; the probe accepted any of [200, 500, 503], so read the status before the timing
**Risk:**
- filters that cannot use an index (`path_part`, `title_part`); each forces a full scan
- deep paging has a hard ceiling: from + size is bounded by the index's max_result_window (10,000 by default); beyond that the query is rejected outright rather than being slow
- ordering is not stable under concurrent writes: Elasticsearch from/size over a relevance sort; a concurrent index write can shift scores and therefore row positions, so a row can be skipped or repeated across pages
- probe `search-transcripts-page-1` failed: expected status [200], got 503
- probe `search-transcripts-deep-page` failed: expected status [200], got 503
**UI verdict:** Safe for a search-results panel showing the first few pages (worst-case p95 3ms), and not safe for anything that scrolls indefinitely: the offset window is finite and the ordering is by relevance, so a row can move between pages while the user is reading. Cap the result set in the UI at a few hundred and offer refinement rather than more pages.

### GET /api/streams

**Purpose:** Read Streams
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.streams.read_streams`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[StreamRead]` (rows are `StreamRead`)
Fields: `id: int`, `stream_index?: int | None`, `codec_type: str`, `codec_name?: str | None`, `language?: str | None`, `width?: int | None`, `height?: int | None`, `frame_rate?: float | None`, `channels?: int | None`, `sample_rate?: int | None`, `is_default?: bool | None`, `is_forced?: bool | None`, `title?: str | None`, `asset_id: int`

**Queries issued:**
- `execute` on `streams` in `SQLAlchemyStreamRepository.list_all` (app/repositories/stream_repository.py:28)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `streams` 65,745 rows · `title` 22% filled · `frame_rate` 29% filled · `height` 29% filled · `width` 29% filled · `channels` 32% filled · `sample_rate` 32% filled · `language` 92% filled · `codec_name` 100% filled
**Collection sizes:** `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `streams-all` (`/api/streams`): p50 1541ms · p95 1856ms · 15.15MB · 65739 items · n=7 — no pagination at all; the response is every stream row in the database
**Risk:**
- no pagination and no page-size cap; the largest collection measured is 65,739 rows (measured directly from a probe response)
- measured p95 of 1856ms on `streams-all` is a page-load-scale wait
- largest measured payload is 15.2MB (`streams-all`)
**UI verdict:** Not safe to render directly: the largest collection is 65,739 rows (measured directly from a probe response), measured p95 1856ms, returned in a single uncapped response. Usable for a count or a preview of the first few, but a screen that lists these needs either a paginated endpoint or client-side virtualisation plus the acceptance that the whole payload crosses the wire first.

### GET /api/streams/{stream_id}

**Purpose:** Read Stream
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.streams.read_stream`
**Pagination:** none — single object; not a collection

**Response 200:** `StreamRead`
Fields: `id: int`, `stream_index?: int | None`, `codec_type: str`, `codec_name?: str | None`, `language?: str | None`, `width?: int | None`, `height?: int | None`, `frame_rate?: float | None`, `channels?: int | None`, `sample_rate?: int | None`, `is_default?: bool | None`, `is_forced?: bool | None`, `title?: str | None`, `asset_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `stream_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `streams` in `SQLAlchemyStreamRepository.get` (app/repositories/stream_repository.py:21)

**Limits:** none declared

**Data shape:** `streams` 65,745 rows · `title` 22% filled · `frame_rate` 29% filled · `height` 29% filled · `width` 29% filled · `channels` 32% filled · `sample_rate` 32% filled · `language` 92% filled · `codec_name` 100% filled
**Collection sizes:** `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### PATCH /api/streams/{stream_id}

**Purpose:** Update Stream
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.streams.update_stream`
**Pagination:** none — single object; not a collection

**Response 200:** `StreamRead`
Fields: `id: int`, `stream_index?: int | None`, `codec_type: str`, `codec_name?: str | None`, `language?: str | None`, `width?: int | None`, `height?: int | None`, `frame_rate?: float | None`, `channels?: int | None`, `sample_rate?: int | None`, `is_default?: bool | None`, `is_forced?: bool | None`, `title?: str | None`, `asset_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | StreamPatchPublic | n/a | — |
| `stream_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `streams` in `SQLAlchemyStreamRepository.update` (app/repositories/stream_repository.py:32)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `streams` in `SQLAlchemyStreamRepository.update` (app/repositories/stream_repository.py:43)

**Limits:** none declared

**Data shape:** `streams` 65,745 rows · `title` 22% filled · `frame_rate` 29% filled · `height` 29% filled · `width` 29% filled · `channels` 32% filled · `sample_rate` 32% filled · `language` 92% filled · `codec_name` 100% filled
**Collection sizes:** `streams` per `assets` — min 1 / median 3 / p95 20 / max 79 (13,171 of 13,321 parents have any)
**Facet candidates:** `streams.codec_name` — 21 distinct; values withheld (see `--include-example-values`) · `streams.codec_type` — 4 distinct; values withheld (see `--include-example-values`) · `streams.is_default` — 1 distinct; values withheld (see `--include-example-values`) · `streams.is_forced` — 1 distinct; values withheld (see `--include-example-values`) · `streams.language` — 60 distinct; values withheld (see `--include-example-values`) · `streams.title` — 12 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/tags

**Purpose:** Get Tags
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.get_tags`
**Pagination:** keyset, default 50, cap 500, ordering stable under concurrent writes (cursor is a keyset bookmark and normalize_sort() (app/utils/sorting.py) always appends `id` as a final tie-breaker, so ordering is total and a concurrent insert cannot shift rows across an already-issued page boundary)

**Response 200:** `PaginatedResponse_TagRead_` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `after` | query | str \| None | n/a | — |
| `before` | query | str \| None | n/a | — |
| `include` | query | str \| None | n/a | — |
| `limit` | query | int (maximum=500, minimum=1) | n/a | — |
| `name` | query | str \| None | **no** | substring match on tags.name; the pattern has a leading wildcard, which a btree cannot use at all, so this is a sequential scan regardless of indexing (a trigram index would be needed) |
| `sort` | query | str | n/a | — |
| `sort=color` | sort | — | **no** | no index on tags.color; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=id` | sort | — | yes (`tags_pkey`) | served by tags_pkey |
| `sort=name` | sort | — | yes (`ix_tags_name`) | served by ix_tags_name |

**Queries issued:**
- `select_page` on `tags` in `SQLAlchemyTagRepository.list_paged` (app/repositories/tag_repository.py:112)
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47)

**Limits:** `limit` <= 500; `limit` >= 1

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `tags-page-1` (`/api/tags?limit=50`): p50 102ms · p95 120ms · 7.2KB · 39 items · n=7
**Risk:**
- unindexed sort keys (`color`); every page sorts the whole filtered set, but `tags` holds only 43 rows, so this is latent rather than live -- the measured cost of an unindexed sort is currently indistinguishable from an indexed one. It becomes real as the table grows
- filters that cannot use an index (`name`); each forces a sequential scan, though `tags` holds only 43 rows, so the scan is currently cheap. This is a constraint on how large the table can grow before search becomes the bottleneck, not a live cost
**UI verdict:** Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500. Caveats: expect the sort control to be the expensive part.

### POST /api/tags

**Purpose:** Create Tag
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.create_tag`
**Pagination:** none — single object; not a collection

**Response 201:** `TagRead`
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagCreatePublic | n/a | — |

**Queries issued:**
- `add` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:41)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:43)
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/tags/{tag_id}

**Purpose:** Get Tag
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.get_tag`
**Pagination:** none — single object; not a collection

**Response 200:** `TagRead`
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `tag_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `tags` in `SQLAlchemyTagRepository.get` (app/repositories/tag_repository.py:50)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `tag-detail` (`/api/tags/1`): p50 96ms · p95 103ms · 212B · n=7
**Risk:** none identified by this run.
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 103ms).

### PATCH /api/tags/{tag_id}

**Purpose:** Update Tag
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.update_tag`
**Pagination:** none — single object; not a collection

**Response 200:** `TagRead`
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagPatchPublic | n/a | — |
| `tag_id` | path | int | n/a | — |
| `tags.id` | lookup | — | yes (`tags_pkey`) | served by tags_pkey |

**Queries issued:**
- `execute` on `tags` in `SQLAlchemyTagRepository.update` (app/repositories/tag_repository.py:61)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyTagRepository.update` (app/repositories/tag_repository.py:72)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PUT /api/tags/{tag_id}

**Purpose:** Update Tag By Put
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.update_tag_by_put`
**Pagination:** none — single object; not a collection

**Response 200:** `TagRead`
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagPatchPublic | n/a | — |
| `tag_id` | path | int | n/a | — |
| `tags.id` | lookup | — | yes (`tags_pkey`) | served by tags_pkey |

**Queries issued:**
- `execute` on `tags` in `SQLAlchemyTagRepository.update` (app/repositories/tag_repository.py:61)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyTagRepository.update` (app/repositories/tag_repository.py:72)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/tags/{tag_id}/tags

**Purpose:** Get Child Tags
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.get_child_tags`
**Pagination:** keyset, default 50, cap 500, ordering stable under concurrent writes (cursor is a keyset bookmark and normalize_sort() (app/utils/sorting.py) always appends `id` as a final tie-breaker, so ordering is total and a concurrent insert cannot shift rows across an already-issued page boundary)

**Response 200:** `PaginatedResponse_TagRead_` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `tag_id` | path | int | n/a | — |
| `after` | query | str \| None | n/a | — |
| `before` | query | str \| None | n/a | — |
| `include` | query | str \| None | n/a | — |
| `limit` | query | int (maximum=500, minimum=1) | n/a | — |
| `name` | query | str \| None | **no** | substring match on tags.name; the pattern has a leading wildcard, which a btree cannot use at all, so this is a sequential scan regardless of indexing (a trigram index would be needed) |
| `sort` | query | str | n/a | — |
| `sort=color` | sort | — | **no** | no index on tags.color; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=id` | sort | — | yes (`tags_pkey`) | served by tags_pkey |
| `sort=name` | sort | — | yes (`ix_tags_name`) | served by ix_tags_name |

**Queries issued:**
- `select_page` on `tags` in `SQLAlchemyTagRepository.list_paged` (app/repositories/tag_repository.py:112)
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47)

**Limits:** `limit` <= 500; `limit` >= 1

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed sort keys (`color`); every page sorts the whole filtered set, but `tags` holds only 43 rows, so this is latent rather than live -- the measured cost of an unindexed sort is currently indistinguishable from an indexed one. It becomes real as the table grows
- filters that cannot use an index (`name`); each forces a sequential scan, though `tags` holds only 43 rows, so the scan is currently cheap. This is a constraint on how large the table can grow before search becomes the bottleneck, not a live cost
**UI verdict:** UNKNOWN — cursor pagination means deep pages do not degrade the way offset does, but no timings were taken, so first-screen cost is unmeasured; see Gaps.

### POST /api/tags/{tag_id}/tags

**Purpose:** Create Child Tag
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.tags.create_child_tag`
**Pagination:** none — single object; not a collection

**Response 201:** `TagRead`
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagCreatePublic | n/a | — |
| `tag_id` | path | int | n/a | — |

**Queries issued:**
- `add` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:41)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:43)
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/title_types

**Purpose:** Get Title Types
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.title_types.get_title_types`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TitleTypeRead]` (rows are `TitleTypeRead`)
Fields: `id: int`, `code: str`, `label: str`, `description?: str | None`

**Queries issued:**
- `execute` on `title_types` in `SQLAlchemyTitleTypeRepository.list_all` (app/repositories/title_type_repository.py:37)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `title_types` 8 rows · `description` 0% filled
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no pagination, but the largest collection measured is only 8 rows (all 8 rows of `title_types`), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 8 rows (all 8 rows of `title_types`). The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/title_types

**Purpose:** Create Title Type
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.title_types.create_title_type`
**Pagination:** none — single object; not a collection

**Response 201:** `TitleTypeRead`
Fields: `id: int`, `code: str`, `label: str`, `description?: str | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleTypeCreatePublic | n/a | — |

**Queries issued:**
- `add` on `title_types` in `SQLAlchemyTitleTypeRepository.create` (app/repositories/title_type_repository.py:19)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `title_types` in `SQLAlchemyTitleTypeRepository.create` (app/repositories/title_type_repository.py:21)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `description` 0% filled
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/title_types/{title_type_id}

**Purpose:** Delete Title Type
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.title_types.delete_title_type`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - the title type is still used by one or more titles, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_type_id` | path | int | n/a | — |
| `titles.title_type_id` | lookup | — | yes (`ix_titles_title_type_id`) | served by ix_titles_title_type_id |

**Queries issued:**
- `execute` on `titles` in `SQLAlchemyTitleTypeRepository.usage_count` (app/repositories/title_type_repository.py:70)
- `get` on `title_types` in `SQLAlchemyTitleTypeRepository.delete` (app/repositories/title_type_repository.py:58)
- `execute` on `title_types` in `SQLAlchemyTitleTypeRepository.delete` (app/repositories/title_type_repository.py:61)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `title_types` in `SQLAlchemyTitleTypeRepository.exists` (app/repositories/title_type_repository.py:29)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `titles` 1,573 rows
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/title_types/{title_type_id}

**Purpose:** Get Title Type
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.title_types.get_title_type`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleTypeRead`
Fields: `id: int`, `code: str`, `label: str`, `description?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_type_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `title_types` in `SQLAlchemyTitleTypeRepository.get` (app/repositories/title_type_repository.py:25)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `description` 0% filled
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Likely safe for a detail view — the read is index-covered — but it was not timed; see Gaps.

### PATCH /api/title_types/{title_type_id}

**Purpose:** Update Title Type
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.title_types.update_title_type`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleTypeRead`
Fields: `id: int`, `code: str`, `label: str`, `description?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleTypePatchPublic | n/a | — |
| `title_type_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `title_types` in `SQLAlchemyTitleTypeRepository.update` (app/repositories/title_type_repository.py:45)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `title_types` in `SQLAlchemyTitleTypeRepository.update` (app/repositories/title_type_repository.py:54)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `description` 0% filled
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/

**Purpose:** Read Titles
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.read_titles`
**Pagination:** keyset, default 50, cap 500, ordering stable under concurrent writes (cursor is a keyset bookmark and normalize_sort() (app/utils/sorting.py) always appends `id` as a final tie-breaker, so ordering is total and a concurrent insert cannot shift rows across an already-issued page boundary)
**Trailing slash:** required — the same path without it returns a 307 redirect

**Response 200:** `PaginatedResponse_TitleReadExtended_` (rows are `TitleReadExtended`)
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`, `tags?: list[TagRead] | None` *(only with `include=tags`)*, `references?: list[TitleReferenceRead] | None` *(only with `include=references`)*
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `after` | query | str \| None | n/a | — |
| `before` | query | str \| None | n/a | — |
| `include` | query | str \| None | n/a | — |
| `limit` | query | int (maximum=500, minimum=1) | n/a | — |
| `name` | query | str \| None | **no** | substring match on titles.name; the pattern has a leading wildcard, which a btree cannot use at all, so this is a sequential scan regardless of indexing (a trigram index would be needed) |
| `sort` | query | str | n/a | — |
| `sort=id` | sort | — | yes (`titles_pkey`) | served by titles_pkey |
| `sort=name` | sort | — | **no** | no index on titles.name; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=title_type` | sort | — | yes (`ix_title_types_code`) | served by ix_title_types_code (ordered by `title_types.code` through a join, not by a column on `titles`) |

**Queries issued:**
- `select_page` on `titles` in `SQLAlchemyTitleRepository.list_paged` (app/repositories/title_repository.py:69)

**Limits:** `limit` <= 500; `limit` >= 1

**Data shape:** `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled · fill rate not reported for `references`, `tags` (returned empty unless `include=` asks for them)
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Measured:**
- `titles-page-1` (`/api/titles/?limit=50`): p50 110ms · p95 118ms · 35.5KB · 50 items · n=7
- `titles-name-substring` (`/api/titles/?limit=50&name=e`): p50 108ms · p95 111ms · 37.3KB · 50 items · n=7 — leading-wildcard name search, the broadest a UI search box can send
- `titles-deep-page` (`/api/titles/?after=>i:1812&limit=50`): p50 94ms · p95 105ms · 49B · 0 items · n=7 — deep page reached by following 40 `page.next` cursors; a keyset endpoint offers no way to jump straight to it
  - the collection ran out before page 41: this page came back empty, so the timing is not comparable with page 1
**Risk:**
- unindexed sort keys (`name`); every page sorts the whole filtered set, but `titles` holds only 1,573 rows, so this is latent rather than live -- the measured cost of an unindexed sort is currently indistinguishable from an indexed one. It becomes real as the table grows
- filters that cannot use an index (`name`); each forces a sequential scan, though `titles` holds only 1,573 rows, so the scan is currently cheap. This is a constraint on how large the table can grow before search becomes the bottleneck, not a live cost
- the trailing slash is required; requesting it without one gets a 307, which a cross-origin fetch with credentials will not always follow
- fields that are empty unless requested, and indistinguishable from genuinely empty: `references`, `tags`
**UI verdict:** Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500. Caveats: expect the sort control to be the expensive part.

### POST /api/titles/

**Purpose:** Create Title
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.create_title`
**Pagination:** none — single object; not a collection
**Trailing slash:** required — the same path without it returns a 307 redirect

**Response 201:** `TitleRead`
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`
Also declares: `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleCreatePublic | n/a | — |
| `title_types.code` | lookup | — | yes (`ix_title_types_code`) | served by ix_title_types_code |

**Queries issued:**
- `execute` on `title_types` in `SQLAlchemyTitleTypeRepository.get_by_code` (app/repositories/title_type_repository.py:33)
- `add` on `titles` in `SQLAlchemyTitleRepository.create` (app/repositories/title_repository.py:27)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `titles` in `SQLAlchemyTitleRepository.create` (app/repositories/title_repository.py:29)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- the trailing slash is required; requesting it without one gets a 307, which a cross-origin fetch with credentials will not always follow
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/by-scheme/{scheme_id}/{external_id}

**Purpose:** Get Title By External Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.get_title_by_external_id`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleRead`
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`
Also declares: `404` No title with that scheme and external ID found, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `external_id` | path | str | n/a | — |
| `scheme_id` | path | int | n/a | — |
| `titles.external_ids` | lookup | — | **no** | any on titles.external_ids cannot use a plain btree index |

**Queries issued:**
- `execute` on `titles` in `SQLAlchemyTitleRepository.get_by_external_id` (app/repositories/title_repository.py:40)

**Limits:** none declared

**Data shape:** `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `titles.external_ids`; the read is a sequential scan
**UI verdict:** UNKNOWN — the read is not index-covered, but was not timed; see Gaps.

### GET /api/titles/{parent_title_id}/contents

**Purpose:** Read Title Contents
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.read_title_contents`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TitleContentReadExtended]` (rows are `TitleContentReadExtended`)
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`, `asset?: AssetRead | None`, `child_title?: TitleRead | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `parent_title_id` | path | int | n/a | — |

**Queries issued:**
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository.list_title_content` (app/repositories/title_content_repository.py:49)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)
- `select` on `assets` in `TitleContentORM.asset (ORM lazy load)` (app/models/ (relationship strategy)) **[N+1]** — lazy='select' relationship TitleContentORM.asset is serialised by the response model; SQLAlchemy emits one SELECT on assets per row returned unless the caller passes include= to eager-load it
- `select` on `titles` in `TitleContentORM.child_title (ORM lazy load)` (app/models/ (relationship strategy)) **[N+1]** — lazy='select' relationship TitleContentORM.child_title is serialised by the response model; SQLAlchemy emits one SELECT on titles per row returned unless the caller passes include= to eager-load it

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `assets` 13,321 rows · `title_contents` 2,135 rows · `titles` 1,573 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `title-contents` (`/api/titles/1/contents`): p50 162ms · p95 186ms · 505B · 1 items · n=7 — unpaginated child list; size is whatever that title happens to hold
**Risk:**
- one extra SELECT per row: TitleContentORM.asset (ORM lazy load) is `lazy='select'` and is serialised into the response, so a page of N rows costs up to N additional queries against `assets`
- one extra SELECT per row: TitleContentORM.child_title (ORM lazy load) is `lazy='select'` and is serialised into the response, so a page of N rows costs up to N additional queries against `titles`
- no pagination, but the largest collection measured is only 1 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 1 rows (measured directly from a probe response), measured p95 186ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/titles/{parent_title_id}/contents

**Purpose:** Link Title Contents
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.link_title_contents`
**Pagination:** none — single object; not a collection

**Response 201:** `TitleContentRead`
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleContentInsert | n/a | — |
| `parent_title_id` | path | int | n/a | — |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.parent_title_id` | lookup | — | yes (`uq_parent_asset_once`) | served by uq_parent_asset_once |

**Queries issued:**
- `get` on `title_contents` in `SQLAlchemyTitleContentRepository._get_order_key` (app/repositories/title_content_repository.py:101)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:155)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:118)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:126)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:136)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:146)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._rebalance` (app/repositories/title_content_repository.py:177)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository.compute_new_order_key` (app/repositories/title_content_repository.py:212)
- `add` on `title_contents` in `SQLAlchemyTitleContentRepository.create` (app/repositories/title_content_repository.py:25)
- `refresh` on `title_contents` in `SQLAlchemyTitleContentRepository.create` (app/repositories/title_content_repository.py:27)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** none declared

**Data shape:** `title_contents` 2,135 rows · `titles` 1,573 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `title_contents.order_key`, `title_contents.order_key`, `title_contents.order_key`; the read is a sequential scan
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### POST /api/titles/{parent_title_id}/contents/positioned

**Purpose:** Create Title Content Positioned
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.create_title_content_positioned`
**Pagination:** none — single object; not a collection

**Response 201:** `TitleContentRead`
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleContentInsert | n/a | — |
| `parent_title_id` | path | int | n/a | — |
| `after_id` | query | int \| None | n/a | Place after this id |
| `before_id` | query | int \| None | n/a | Place before this id |
| `position` | query | str \| None | n/a | Special position: 'start' or 'end' |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.parent_title_id` | lookup | — | yes (`uq_parent_asset_once`) | served by uq_parent_asset_once |

**Queries issued:**
- `get` on `title_contents` in `SQLAlchemyTitleContentRepository._get_order_key` (app/repositories/title_content_repository.py:101)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:155)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:118)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:126)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:136)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:146)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._rebalance` (app/repositories/title_content_repository.py:177)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository.compute_new_order_key` (app/repositories/title_content_repository.py:212)
- `add` on `title_contents` in `SQLAlchemyTitleContentRepository.create` (app/repositories/title_content_repository.py:25)
- `refresh` on `title_contents` in `SQLAlchemyTitleContentRepository.create` (app/repositories/title_content_repository.py:27)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** none declared

**Data shape:** `title_contents` 2,135 rows · `titles` 1,573 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `title_contents.order_key`, `title_contents.order_key`, `title_contents.order_key`; the read is a sequential scan
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/titles/{parent_title_id}/contents/{title_contents_id}

**Purpose:** Unlink Title Contents
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.unlink_title_contents`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `parent_title_id` | path | int | n/a | — |
| `title_contents_id` | path | int | n/a | — |
| `title_contents.id` | lookup | — | yes (`title_contents_pkey`) | served by title_contents_pkey |

**Queries issued:**
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository.delete_title_content` (app/repositories/title_content_repository.py:74)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)

**Limits:** none declared

**Data shape:** `title_contents` 2,135 rows · `titles` 1,573 rows
**Collection sizes:** `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/titles/{parent_title_id}/contents/{title_contents_id}

**Purpose:** Partial Title Content Update
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.partial_title_content_update`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleContentRead`
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleContentPatchPublic | n/a | — |
| `parent_title_id` | path | int | n/a | — |
| `title_contents_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `title_contents` in `SQLAlchemyTitleContentRepository.update` (app/repositories/title_content_repository.py:58)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `title_contents` in `SQLAlchemyTitleContentRepository.update` (app/repositories/title_content_repository.py:69)

**Limits:** none declared

**Data shape:** `title_contents` 2,135 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/titles/{parent_title_id}/contents/{title_contents_id}/reorder

**Purpose:** Reorder Title Content
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.contents.reorder_title_content`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleContentRead`
Fields: `id: int`, `kind: ContentKind`, `child_title_id?: int | None`, `asset_id?: int | None`, `label?: str | None`, `parent_title_id: int`, `order_key: str`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `parent_title_id` | path | int | n/a | — |
| `title_contents_id` | path | int | n/a | — |
| `after_id` | query | int \| None | n/a | Place after this id |
| `before_id` | query | int \| None | n/a | Place before this id |
| `position` | query | str \| None | n/a | Special position: 'start' or 'end' |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.order_key` | lookup | — | **no** | no index on title_contents.order_key; requires a sequential scan |
| `title_contents.parent_title_id` | lookup | — | yes (`uq_parent_asset_once`) | served by uq_parent_asset_once |

**Queries issued:**
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)
- `get` on `title_contents` in `SQLAlchemyTitleContentRepository.reorder` (app/repositories/title_content_repository.py:260)
- `get` on `title_contents` in `SQLAlchemyTitleContentRepository._get_order_key` (app/repositories/title_content_repository.py:101)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:155)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:118)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:126)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:136)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._get_prev_next_keys` (app/repositories/title_content_repository.py:146)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository._rebalance` (app/repositories/title_content_repository.py:177)
- `execute` on `title_contents` in `SQLAlchemyTitleContentRepository.compute_new_order_key` (app/repositories/title_content_repository.py:212)
- `refresh` on `title_contents` in `SQLAlchemyTitleContentRepository.reorder` (app/repositories/title_content_repository.py:272)

**Limits:** none declared

**Data shape:** `title_contents` 2,135 rows · `titles` 1,573 rows · `child_title_id` 36% filled · `asset_id` 64% filled · `label` 99% filled
**Collection sizes:** `title_contents` per `assets` — min 1 / median 1 / p95 1 / max 1 (1,376 of 13,321 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_contents` per `titles` — min 1 / median 2 / p95 2 / max 2 (500 of 1,573 parents have any) · `title_contents` per `titles` — min 1 / median 1 / p95 2 / max 35 (1,561 of 1,573 parents have any)
**Facet candidates:** `title_contents.kind` — 2 distinct; values withheld (see `--include-example-values`) · `title_contents.order_key` — 46 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `title_contents.order_key`, `title_contents.order_key`, `title_contents.order_key`; the read is a sequential scan
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/{title_id}

**Purpose:** Read Title
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.read_title`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleRead`
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `titles` in `SQLAlchemyTitleRepository.get` (app/repositories/title_repository.py:33)

**Limits:** none declared

**Data shape:** `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Measured:**
- `title-detail` (`/api/titles/1`): p50 92ms · p95 110ms · 601B · n=7
**Risk:** none identified by this run.
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 110ms).

### PATCH /api/titles/{title_id}

**Purpose:** Partial Title Update
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.partial_title_update`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleRead`
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitlePatchPublic | n/a | — |
| `title_id` | path | int | n/a | — |
| `titles.id` | lookup | — | yes (`titles_pkey`) | served by titles_pkey |
| `title_types.code` | lookup | — | yes (`ix_title_types_code`) | served by ix_title_types_code |

**Queries issued:**
- `execute` on `titles` in `SQLAlchemyTitleRepository.update` (app/repositories/title_repository.py:84)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyTitleRepository.update` (app/repositories/title_repository.py:95)
- `execute` on `title_types` in `SQLAlchemyTitleTypeRepository.get_by_code` (app/repositories/title_type_repository.py:33)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PUT /api/titles/{title_id}

**Purpose:** Update Title
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.core.update_title`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleRead`
Fields: `id: int`, `name: str`, `title_type: str`, `release_year?: int | None`, `synopsis?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitlePatchPublic | n/a | — |
| `title_id` | path | int | n/a | — |
| `titles.id` | lookup | — | yes (`titles_pkey`) | served by titles_pkey |
| `title_types.code` | lookup | — | yes (`ix_title_types_code`) | served by ix_title_types_code |

**Queries issued:**
- `execute` on `titles` in `SQLAlchemyTitleRepository.update` (app/repositories/title_repository.py:84)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyTitleRepository.update` (app/repositories/title_repository.py:95)
- `execute` on `title_types` in `SQLAlchemyTitleTypeRepository.get_by_code` (app/repositories/title_type_repository.py:33)

**Limits:** none declared

**Data shape:** `title_types` 8 rows · `titles` 1,573 rows · `release_year` 55% filled · `synopsis` 96% filled
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `title_types.code` — 8 distinct; values withheld (see `--include-example-values`) · `title_types.label` — 8 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/{title_id}/ids

**Purpose:** Get Title Ids
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.external_ids.get_title_ids`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[ExternalIdentifierReadExtended]` (rows are `ExternalIdentifierReadExtended`)
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`, `scheme?: IdSchemeRead | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `execute` on `id_schemes`, `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.list_for_entity` (app/repositories/external_identifier_repository.py:84) **[N+1]** — comprehension
- `select` on `id_schemes` in `ExternalIdentifierORM.scheme (ORM lazy load)` (app/models/ (relationship strategy)) **[N+1]** — lazy='select' relationship ExternalIdentifierORM.scheme is serialised by the response model; SQLAlchemy emits one SELECT on id_schemes per row returned

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `external_identifiers` 1,490 rows · `id_schemes` 3 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`) · `id_schemes.code` — 3 distinct; values withheld (see `--include-example-values`) · `id_schemes.label` — 3 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- one extra SELECT per row: ExternalIdentifierORM.scheme (ORM lazy load) is `lazy='select'` and is serialised into the response, so a page of N rows costs up to N additional queries against `id_schemes`
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyExternalIdentifierRepository.list_for_entity (comprehension)
- no pagination and no page-size cap; the largest collection measured is 701 rows (external_identifiers.scheme_id -> id_schemes)
**UI verdict:** Not safe to render directly: the largest collection is 701 rows (external_identifiers.scheme_id -> id_schemes), returned in a single uncapped response. Usable for a count or a preview of the first few, but a screen that lists these needs either a paginated endpoint or client-side virtualisation plus the acceptance that the whole payload crosses the wire first.

### POST /api/titles/{title_id}/ids

**Purpose:** Create Title Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.external_ids.create_title_id`
**Pagination:** none — single object; not a collection

**Response 201:** `ExternalIdentifierRead`
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`
Also declares: `404` Title not found, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | ExternalIdentifierCreatePublic | n/a | — |
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `assets` in `SQLAlchemyMediaRepository.exists` (app/repositories/media_repository.py:52)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)
- `add` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.create` (app/repositories/external_identifier_repository.py:90)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.create` (app/repositories/external_identifier_repository.py:92)

**Limits:** none declared

**Data shape:** `assets` 13,321 rows · `external_identifiers` 1,490 rows · `titles` 1,573 rows
**Collection sizes:** `assets` per `assets` — min 1 / median 1 / p95 4 / max 4 (6 of 13,321 parents have any) · `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Facet candidates:** `assets.container_format` — 4 distinct; values withheld (see `--include-example-values`) · `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### DELETE /api/titles/{title_id}/ids/{record_id}

**Purpose:** Delete Title Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.external_ids.delete_title_id`
**Pagination:** none — single object; not a collection

**Response 204:** `no model declared`
Also declares: `404` External ID not found or doesn't belong to this title, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `record_id` | path | int | n/a | — |
| `title_id` | path | int | n/a | — |
| `external_identifiers.id` | lookup | — | yes (`external_identifiers_pkey`) | served by external_identifiers_pkey |

**Queries issued:**
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.get` (app/repositories/external_identifier_repository.py:66)
- `execute` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.delete` (app/repositories/external_identifier_repository.py:114)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)

**Limits:** none declared

**Data shape:** `external_identifiers` 1,490 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/titles/{title_id}/ids/{record_id}

**Purpose:** Update Title Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.external_ids.update_title_id`
**Pagination:** none — single object; not a collection

**Response 200:** `ExternalIdentifierRead`
Fields: `id: int`, `external_id: str`, `scheme_id: int`, `entity_type: EntityTypeEnum`, `entity_id: int`, `created_at: str(date-time)`
Also declares: `404` External ID not found or doesn't belong to this title, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | ExternalIdentifierPatchPublic | n/a | — |
| `record_id` | path | int | n/a | — |
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.get` (app/repositories/external_identifier_repository.py:66)
- `get` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.update` (app/repositories/external_identifier_repository.py:99)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `external_identifiers` in `SQLAlchemyExternalIdentifierRepository.update` (app/repositories/external_identifier_repository.py:108)

**Limits:** none declared

**Data shape:** `external_identifiers` 1,490 rows
**Collection sizes:** `external_identifiers` per `id_schemes` — min 132 / median 657 / p95 701 / max 701 (3 of 3 parents have any)
**Facet candidates:** `external_identifiers.entity_type` — 2 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/{title_id}/references

**Purpose:** Read Title References
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.references.read_title_references`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TitleReferenceRead]` (rows are `TitleReferenceRead`)
Fields: `id: int`, `reference_type: TitleReferenceTypeEnum`, `reference_url: str`, `label?: str | None`, `title_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_id` | path | int | n/a | — |
| `title_references.title_id` | lookup | — | **no** | no index on title_references.title_id; requires a sequential scan |

**Queries issued:**
- `execute` on `title_references` in `SQLAlchemyTitleReferenceRepository.list_title_references` (app/repositories/title_reference_repository.py:32)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `title_references` 0 rows · `titles` 1,573 rows
**Collection sizes:** `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `title_references.title_id`; the read is a sequential scan
- no pagination and no page-size cap; the largest collection measured is 727 rows (titles.title_type_id -> title_types)
**UI verdict:** Not safe to render directly: the largest collection is 727 rows (titles.title_type_id -> title_types), returned in a single uncapped response. Usable for a count or a preview of the first few, but a screen that lists these needs either a paginated endpoint or client-side virtualisation plus the acceptance that the whole payload crosses the wire first.

### POST /api/titles/{title_id}/references

**Purpose:** Create Title Reference
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.references.create_title_reference`
**Pagination:** none — single object; not a collection

**Response 201:** `TitleReferenceRead`
Fields: `id: int`, `reference_type: TitleReferenceTypeEnum`, `reference_url: str`, `label?: str | None`, `title_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleReferenceCreatePublic | n/a | — |
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `add` on `title_references` in `SQLAlchemyTitleReferenceRepository.create` (app/repositories/title_reference_repository.py:19)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `title_references` in `SQLAlchemyTitleReferenceRepository.create` (app/repositories/title_reference_repository.py:21)

**Limits:** none declared

**Data shape:** `title_references` 0 rows
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/titles/{title_id}/references/{reference_id}

**Purpose:** Partial Title Reference Update
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.references.partial_title_reference_update`
**Pagination:** none — single object; not a collection

**Response 200:** `TitleReferenceRead`
Fields: `id: int`, `reference_type: TitleReferenceTypeEnum`, `reference_url: str`, `label?: str | None`, `title_id: int`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TitleReferencePatchPublic | n/a | — |
| `reference_id` | path | int | n/a | — |
| `title_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `title_references` in `SQLAlchemyTitleReferenceRepository.update` (app/repositories/title_reference_repository.py:42)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `title_references` in `SQLAlchemyTitleReferenceRepository.update` (app/repositories/title_reference_repository.py:53)

**Limits:** none declared

**Data shape:** `title_references` 0 rows
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/titles/{title_id}/tags

**Purpose:** Get Title Tags
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.tags.get_title_tags`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TagRead]` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `title_id` | path | int | n/a | — |
| `title_tags.title_id` | lookup | — | yes (`ix_title_tags_title_id`) | served by ix_title_tags_title_id |

**Queries issued:**
- `execute` on `tags`, `title_tags` in `SQLAlchemyTagRepository.get_title_tags` (app/repositories/tag_repository.py:218) **[N+1]** — comprehension
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `tags` 43 rows · `title_tags` 840 rows · `titles` 1,573 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any) · `title_tags` per `tags` — min 132 / median 132 / p95 708 / max 708 (2 of 43 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_tags` per `titles` — min 1 / median 1 / p95 1 / max 1 (840 of 1,573 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `title-tags` (`/api/titles/1/tags`): p50 119ms · p95 141ms · 2B · 0 items · n=7
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyTagRepository.get_title_tags (comprehension)
- no pagination, but the largest collection measured is only 0 rows (measured directly from a probe response), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 0 rows (measured directly from a probe response), measured p95 141ms. The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### POST /api/titles/{title_id}/tags

**Purpose:** Tag Title By Name
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.tags.tag_title_by_name`
**Pagination:** none — single object; not a collection

**Response 200:** `TaggingReport`
Fields: `added_tags: list[TagRead]`, `tagging_errors: list[str]`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagNameSet | n/a | — |
| `title_id` | path | int | n/a | — |
| `tags.name` | lookup | — | yes (`ix_tags_name`) | served by ix_tags_name |
| `title_tags.title_id` | lookup | — | yes (`ix_title_tags_title_id`) | served by ix_title_tags_title_id |

**Queries issued:**
- `execute` on `tags` in `SQLAlchemyTagRepository.get_by_name` (app/repositories/tag_repository.py:56) **[N+1]** — comprehension
- `add` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:41) **[N+1]** — for tag_name in unique_names
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35) **[N+1]** — for tag_name in unique_names
- `refresh` on `tags` in `SQLAlchemyTagRepository.create` (app/repositories/tag_repository.py:43) **[N+1]** — for tag_name in unique_names
- `get` on `tags` in `SQLAlchemyTagRepository.exists` (app/repositories/tag_repository.py:47) **[N+1]** — for tag_name in unique_names
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `tags` in `SQLAlchemyTagRepository.get` (app/repositories/tag_repository.py:50) **[N+1]** — for tag_id in tag_ids
- `execute` on `title_tags` in `SQLAlchemyTagRepository.add_title_tags` (app/repositories/tag_repository.py:178)
- `add` on `title_tags` in `SQLAlchemyTagRepository.add_title_tags` (app/repositories/tag_repository.py:186) **[N+1]** — for tag_id in tag_ids
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** none declared

**Data shape:** `tags` 43 rows · `title_tags` 840 rows · `titles` 1,573 rows
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any) · `title_tags` per `tags` — min 132 / median 132 / p95 708 / max 708 (2 of 43 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_tags` per `titles` — min 1 / median 1 / p95 1 / max 1 (840 of 1,573 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyBaseRepository._safe_commit (for tag_name in unique_names); SQLAlchemyTagRepository.add_title_tags (for tag_id in tag_ids); SQLAlchemyTagRepository.create (for tag_name in unique_names); SQLAlchemyTagRepository.exists (for tag_name in unique_names); SQLAlchemyTagRepository.get (for tag_id in tag_ids); SQLAlchemyTagRepository.get_by_name (comprehension)
**UI verdict:** Write path. Safe to call from a form submit, but the work is proportional to the size of the payload rather than constant — send small batches and show a determinate progress state rather than a spinner.

### PUT /api/titles/{title_id}/tags

**Purpose:** Tag Title
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.tags.tag_title`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TagRead]` (rows are `TagRead`)
Fields: `id: int`, `name: str`, `description?: str | None`, `color?: str`, `parent_id?: int | None`, `created_at: str(date-time)`, `updated_at: str(date-time)`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TagSet | n/a | — |
| `title_id` | path | int | n/a | — |
| `title_tags.title_id` | lookup | — | yes (`ix_title_tags_title_id`) | served by ix_title_tags_title_id |

**Queries issued:**
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `get` on `tags` in `SQLAlchemyTagRepository.get` (app/repositories/tag_repository.py:50) **[N+1]** — for tag_id in tag_ids
- `execute` on `title_tags` in `SQLAlchemyTagRepository.add_title_tags` (app/repositories/tag_repository.py:178)
- `add` on `title_tags` in `SQLAlchemyTagRepository.add_title_tags` (app/repositories/tag_repository.py:186) **[N+1]** — for tag_id in tag_ids
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** returns an unbounded list; the response grows with the number of related rows

**Data shape:** `tags` 43 rows · `title_tags` 840 rows · `titles` 1,573 rows · `parent_id` 9% filled
**Collection sizes:** `tags` per `tags` — min 4 / median 4 / p95 4 / max 4 (1 of 43 parents have any) · `title_tags` per `tags` — min 132 / median 132 / p95 708 / max 708 (2 of 43 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_tags` per `titles` — min 1 / median 1 / p95 1 / max 1 (840 of 1,573 parents have any)
**Facet candidates:** `tags.color` — 4 distinct; values withheld (see `--include-example-values`) · `tags.description` — 6 distinct; values withheld (see `--include-example-values`) · `tags.name` — 43 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- queries issued inside a loop, so cost grows with the size of the request: SQLAlchemyTagRepository.add_title_tags (for tag_id in tag_ids); SQLAlchemyTagRepository.get (for tag_id in tag_ids)
- no pagination and no page-size cap; the largest collection measured is 727 rows (titles.title_type_id -> title_types)
**UI verdict:** Write path. Safe to call from a form submit, but the work is proportional to the size of the payload rather than constant — send small batches and show a determinate progress state rather than a spinner.

### DELETE /api/titles/{title_id}/tags/{tag_id}

**Purpose:** Untag Title By Id
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.titles.tags.untag_title_by_id`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`
Also declares: `204` Title exists, but did not have the tag, `404` Title does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `tag_id` | path | int | n/a | — |
| `title_id` | path | int | n/a | — |
| `title_tags.tag_id` | lookup | — | yes (`ix_title_tags_tag_id`) | served by ix_title_tags_tag_id |
| `title_tags.title_id` | lookup | — | yes (`ix_title_tags_title_id`) | served by ix_title_tags_title_id |

**Queries issued:**
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `title_tags` in `SQLAlchemyTagRepository.remove_title_tag` (app/repositories/tag_repository.py:199)
- `get` on `titles` in `SQLAlchemyTitleRepository.exists` (app/repositories/title_repository.py:44)

**Limits:** none declared

**Data shape:** `title_tags` 840 rows · `titles` 1,573 rows
**Collection sizes:** `title_tags` per `tags` — min 132 / median 132 / p95 708 / max 708 (2 of 43 parents have any) · `titles` per `title_types` — min 11 / median 132 / p95 727 / max 727 (6 of 8 parents have any) · `title_tags` per `titles` — min 1 / median 1 / p95 1 / max 1 (840 of 1,573 parents have any)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/transform_requests

**Purpose:** Read Requests
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.read_requests`
**Pagination:** keyset, default 50, cap 500, ordering stable under concurrent writes (cursor is a keyset bookmark and normalize_sort() (app/utils/sorting.py) always appends `id` as a final tie-breaker, so ordering is total and a concurrent insert cannot shift rows across an already-issued page boundary)

**Response 200:** `PaginatedResponse_TransformRequestReadExpanded_` (rows are `TransformRequestReadExpanded`)
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`, `asset: AssetRead`
Also declares: `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `actioned` | query | bool \| None | **no** | no index on media_transform_requests.actioned; requires a sequential scan |
| `after` | query | str \| None | n/a | — |
| `before` | query | str \| None | n/a | — |
| `limit` | query | int (maximum=500, minimum=1) | n/a | — |
| `outcome` | query | OutcomeEnum \| None | **no** | no index on media_transform_requests.outcome; requires a sequential scan |
| `sort` | query | str | n/a | — |
| `transform_type` | query | str \| None (pattern=^[^\s.]+\.[^\s]+$) | **no** | no index on media_transform_requests.transform_type; requires a sequential scan |
| `worker_assigned` | query | bool \| None | **no** | no index on media_transform_requests.worker; requires a sequential scan |
| `sort=created_at` | sort | — | **no** | no index on media_transform_requests.created_at; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |
| `sort=id` | sort | — | yes (`media_transform_requests_pkey`) | served by media_transform_requests_pkey |
| `sort=processed_at` | sort | — | **no** | no index on media_transform_requests.processed_at; every page must sort the whole filtered set, so the keyset cursor keeps the ordering correct but does not make it cheap |

**Queries issued:**
- `select_page` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.list_paged` (app/repositories/transform_request_repository.py:75)

**Limits:** `limit` <= 500; `limit` >= 1

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `transform-requests-page-1` (`/api/transform_requests?limit=50`): p50 150ms · p95 153ms · 42.1KB · 50 items · n=7
**Risk:**
- unindexed sort keys (`created_at`, `processed_at`); every page sorts the whole filtered set, but `media_transform_requests` holds only 17,851 rows, so this is latent rather than live -- the measured cost of an unindexed sort is currently indistinguishable from an indexed one. It becomes real as the table grows
- filters that cannot use an index (`actioned`, `outcome`, `transform_type`, `worker_assigned`); each forces a sequential scan, though `media_transform_requests` holds only 17,851 rows, so the scan is currently cheap. This is a constraint on how large the table can grow before search becomes the bottleneck, not a live cost
**UI verdict:** Safe for first-screen browse and for virtualised infinite scroll — the cursor holds up at depth and the cap is 500. Caveats: expect the sort control to be the expensive part.

### POST /api/transform_requests/claim

**Purpose:** Claim Next Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.claim_next_request`
**Pagination:** none — single object; not a collection

**Response 200:** `TransformRequestReadExpanded`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`, `asset: AssetRead`
Also declares: `204` No requests available to claim, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TransformRequestClaim | n/a | — |
| `media_transform_requests.actioned` | lookup | — | **no** | no index on media_transform_requests.actioned; requires a sequential scan |
| `media_transform_requests.transform_type` | lookup | — | **no** | no index on media_transform_requests.transform_type; requires a sequential scan |
| `media_transform_requests.worker` | lookup | — | **no** | no index on media_transform_requests.worker; requires a sequential scan |

**Queries issued:**
- `flush` on unresolved table in `SQLAlchemyTransformRequestRepository.claim_next` (app/repositories/transform_request_repository.py:139)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `execute` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.claim_next` (app/repositories/transform_request_repository.py:133)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- unindexed lookup on `media_transform_requests.actioned`, `media_transform_requests.transform_type`, `media_transform_requests.worker`; the read is a sequential scan
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/transform_requests/{request_id}

**Purpose:** Read Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.read_request`
**Pagination:** none — single object; not a collection

**Response 200:** `TransformRequestRead`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `request_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.get` (app/repositories/transform_request_repository.py:33)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:**
- `transform-request-detail` (`/api/transform_requests/4`): p50 103ms · p95 111ms · 411B · n=7
**Risk:** none identified by this run.
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 111ms).

### PATCH /api/transform_requests/{request_id}

**Purpose:** Update Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.update_request`
**Pagination:** none — single object; not a collection

**Response 200:** `TransformRequestRead`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TransformRequestPatchPublic | n/a | — |
| `request_id` | path | int | n/a | — |
| `media_transform_requests.id` | lookup | — | yes (`media_transform_requests_pkey`) | served by media_transform_requests_pkey |

**Queries issued:**
- `execute` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.update` (app/repositories/transform_request_repository.py:94)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on unresolved table in `SQLAlchemyTransformRequestRepository.update` (app/repositories/transform_request_repository.py:105)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### PATCH /api/transform_requests/{request_id}/heartbeat

**Purpose:** Mark Heartbeat
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.mark_heartbeat`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`
Also declares: `400` Heartbeat rejected, `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `request_id` | path | int | n/a | — |
| `media_transform_requests.id` | lookup | — | yes (`media_transform_requests_pkey`) | served by media_transform_requests_pkey |

**Queries issued:**
- `execute` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.mark_heartbeat` (app/repositories/transform_request_repository.py:41)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### POST /api/transform_requests/{request_id}/link

**Purpose:** Create Linked Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.create_linked_request`
**Pagination:** none — single object; not a collection

**Response 201:** `TransformRequestRead`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `<body>` | body | TransformRequestCreatePublic | n/a | — |
| `request_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.get` (app/repositories/transform_request_repository.py:33)
- `add` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:27)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:29)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/transform_requests/{request_id}/logs

**Purpose:** Read Request Logs
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.read_request_logs`
**Pagination:** none — returns the entire collection in one response; there is no page size to be stable across

**Response 200:** `list[TransformRequestLogEntry]` (rows are `TransformRequestLogEntry`)
Fields: `timestamp: str`, `level: str`, `logger?: str | None`, `message: str`, `external_ref?: str | None`
Also declares: `404` Not Found - the requested resource does not exist, `422` Validation Error

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `request_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.get` (app/repositories/transform_request_repository.py:33)

**Limits:** no page size cap and no pagination: the response is the whole collection, however large it is

**Data shape:** `media_transform_requests` 17,851 rows
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:**
- no pagination, but the largest collection measured is only 17 rows (media_transform_requests.asset_id -> assets), so the absent cap is currently latent
**UI verdict:** Safe to render directly: the collection is bounded in practice at 17 rows (media_transform_requests.asset_id -> assets). The absence of a cap is latent rather than live — worth a page size before the data grows, not before the UI ships.

### PATCH /api/transform_requests/{request_id}/retry

**Purpose:** Retry Request
**Auth:** bearer, no scope or role enforced at the route
**Handler:** `app.routers.transform_requests.retry_request`
**Pagination:** none — single object; not a collection

**Response 200:** `TransformRequestRead`
Fields: `id: int`, `transform_type: str`, `parameters?: object | None`, `actioned?: bool`, `processed_at?: str(date-time) | None`, `worker_notes?: str | None`, `external_job_id?: str | None`, `duration?: float | None`, `outcome?: OutcomeEnum | None`, `worker?: str | None`, `on_success?: object | None`, `on_failure?: object | None`, `asset_id: int`, `parent_transform_request_id?: int | None`, `created_at: str(date-time)`, `first_heartbeat?: str(date-time) | None`, `last_heartbeat?: str(date-time) | None`
Also declares: `404` Not Found - the requested resource does not exist, `409` Conflict - unique constraint violated or relationship not permitted, `422` Unprocessable Entity - validation error or database integrity constraint violated, `423` Locked - database is currently in read-only mode

| Param | In | Type | Indexed | Cost note |
|-------|----|------|---------|-----------|
| `request_id` | path | int | n/a | — |

**Queries issued:**
- `get` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.get` (app/repositories/transform_request_repository.py:33)
- `add` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:27)
- `commit` on unresolved table in `SQLAlchemyBaseRepository._safe_commit` (app/repositories/base_repository.py:35)
- `refresh` on `media_transform_requests` in `SQLAlchemyTransformRequestRepository.create` (app/repositories/transform_request_repository.py:29)

**Limits:** none declared

**Data shape:** `media_transform_requests` 17,851 rows · `parent_transform_request_id` 14% filled · `first_heartbeat` 18% filled · `last_heartbeat` 18% filled · `on_failure` 23% filled · `on_success` 23% filled · `worker_notes` 79% filled · `external_job_id` 90% filled · `duration` 97% filled
**Collection sizes:** `media_transform_requests` per `assets` — min 1 / median 1 / p95 3 / max 17 (13,306 of 13,321 parents have any) · `media_transform_requests` per `media_transform_requests` — min 1 / median 2 / p95 2 / max 3 (1,412 of 17,851 parents have any)
**Facet candidates:** `media_transform_requests.actioned` — 1 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.outcome` — 3 distinct; values withheld (see `--include-example-values`) · `media_transform_requests.transform_type` — 11 distinct; values withheld (see `--include-example-values`)
**Measured:** UNKNOWN — no probe covers this endpoint (add one to `probes.yaml`, or Phase 4 was skipped).
**Risk:** none identified by this run.
**UI verdict:** Write path. Single-row work with no loops; safe to drive from a form submit and to treat optimistically, provided the UI reconciles against the response.

### GET /api/version

**Purpose:** Read Version
**Auth:** none (unauthenticated)
**Handler:** `app.app_factory.read_version`
**Pagination:** none — single object; not a collection

**Response 200:** `no model declared`

**Limits:** none declared

**Data shape:** no database tables are read by this endpoint.
**Measured:**
- `version` (`/api/version`): p50 4ms · p95 8ms · 87B · n=7
**Risk:**
- no authentication is required on this route
**UI verdict:** Safe for a detail view and fast enough to prefetch on hover (p95 8ms).

## Candidates for removal

> **Evidence is weak.** No consumer codebase or access log was supplied, so this list is derived from references inside this repository alone. Several endpoints here exist for machine consumers that live elsewhere — the transform-request claim and heartbeat routes are a worker pull queue, and no front end would ever call them. Treat this as a list of things to *ask about*, not a list of things to delete. Re-run with `--frontend-path` or `--access-log` for evidence that can actually support a removal.

| Endpoint | Evidence | Tests referencing it |
|---|---|---|
| `GET /api/health` | weak: no reference found | none |
| `POST /api/jobs` | weak: no reference found | none |
| `PATCH /api/jobs/{job_key}/completed` | weak: no reference found | none |
| `PUT /api/jobs/{job_key}/heartbeat` | weak: no reference found | none |
| `POST /api/log` | weak: no reference found | none |
| `GET /api/titles/by-scheme/{scheme_id}/{external_id}` | weak: no reference found | none |

Nothing has been deleted. This is a list of questions, not actions.

## Gaps

1 value(s) could not be established. Each is listed with the specific thing that would settle it.

| Scope | Not known | What would settle it |
|---|---|---|
| `Phase 5` | whether an endpoint has any real caller | re-run with --frontend-path pointing at a consumer checkout, or --access-log pointing at a log with real traffic; in-repository evidence alone cannot see callers that live in other repositories |

## Index inventory

Every index the coverage judgements above were made against, merged from the SQLAlchemy metadata and the Alembic migrations.

| Table | Index | Columns | Unique | Source |
|---|---|---|---|---|
| `asset_tags` | `asset_tags_pkey` | `asset_id`, `tag_id` | yes | primary key |
| `asset_tags` | `ix_asset_tags_asset_id` | `asset_id` | no | models |
| `asset_tags` | `ix_asset_tags_tag_id` | `tag_id` | no | models |
| `assets` | `assets_path_key` | `path` | yes | column unique=True |
| `assets` | `assets_pkey` | `id` | yes | primary key |
| `assets` | `ix_assets_master_asset_id` | `master_asset_id` | no | models |
| `assets` | `uq_assets` | `path` | yes | unique constraint |
| `external_asset_ids` | `external_asset_ids_pkey` | `id` | yes | primary key |
| `external_asset_ids` | `uq_asset_scheme` | `asset_id`, `scheme_id` | yes | unique constraint |
| `external_asset_ids` | `uq_scheme_external_id` | `scheme_id`, `external_id` | yes | unique constraint |
| `external_identifiers` | `external_identifiers_pkey` | `id` | yes | primary key |
| `external_identifiers` | `ix_external_identifiers_entity` | `entity_type`, `entity_id` | no | models |
| `external_identifiers` | `uq_external_identifier_scheme_id` | `scheme_id`, `external_id` | yes | unique constraint |
| `id_schemes` | `id_schemes_code_key` | `code` | yes | column unique=True |
| `id_schemes` | `id_schemes_pkey` | `id` | yes | primary key |
| `id_schemes` | `ix_id_schemes_code` | `code` | yes | models |
| `jobs` | `jobs_pkey` | `job_key` | yes | primary key |
| `media_transform_requests` | `media_transform_requests_pkey` | `id` | yes | primary key |
| `media_transform_requests` | `uniq_pending_transform_per_asset_and_type` | `asset_id`, `transform_type` WHERE (actioned = false) | yes | models |
| `metadata` | `metadata_pkey` | `id` | yes | primary key |
| `run_summaries` | `run_summaries_pkey` | `id` | yes | primary key |
| `runner_state` | `runner_state_pkey` | `runner_key` | yes | primary key |
| `scanner_run_summaries` | `scanner_run_summaries_pkey` | `id` | yes | primary key |
| `streams` | `streams_pkey` | `id` | yes | primary key |
| `tags` | `ix_tags_name` | `name` | yes | models |
| `tags` | `ix_tags_name_lower` | `lower(tags.name)` | no | models |
| `tags` | `ix_tags_parent_id` | `parent_id` | no | models |
| `tags` | `tags_name_key` | `name` | yes | column unique=True |
| `tags` | `tags_pkey` | `id` | yes | primary key |
| `title_contents` | `title_contents_pkey` | `id` | yes | primary key |
| `title_contents` | `uq_parent_asset_once` | `parent_title_id`, `asset_id` WHERE title_contents.asset_id IS NOT NULL | yes | models |
| `title_contents` | `uq_parent_child_title_once` | `parent_title_id`, `child_title_id` WHERE title_contents.child_title_id IS NOT NULL | yes | models |
| `title_contents` | `uq_parent_order` | `parent_title_id`, `order_key` | yes | unique constraint |
| `title_references` | `title_references_pkey` | `id` | yes | primary key |
| `title_tags` | `ix_title_tags_tag_id` | `tag_id` | no | models |
| `title_tags` | `ix_title_tags_title_id` | `title_id` | no | models |
| `title_tags` | `title_tags_pkey` | `title_id`, `tag_id` | yes | primary key |
| `title_types` | `ix_title_types_code` | `code` | yes | models |
| `title_types` | `title_types_code_key` | `code` | yes | column unique=True |
| `title_types` | `title_types_pkey` | `id` | yes | primary key |
| `titles` | `ix_titles_title_type_id` | `title_type_id` | no | models |
| `titles` | `titles_pkey` | `id` | yes | primary key |
| `asset_tags` | `ix_asset_tags_asset_id` | `asset_id` | no | migration 31d43b7e01c0 |
| `asset_tags` | `ix_asset_tags_tag_id` | `tag_id` | no | migration 31d43b7e01c0 |
| `external_identifiers` | `ix_external_identifiers_entity` | `entity_type`, `entity_id` | no | migration ee9eb74e4b4b |
| `id_schemes` | `ix_id_schemes_code` | `code` | yes | migration 31d43b7e01c0 |
| `media_transform_requests` | `uniq_pending_transform_per_video_and_type` | `video_id`, `transform_type` | yes | migration 31d43b7e01c0 |
| `tags` | `ix_tags_name` | `name` | yes | migration 31d43b7e01c0 |
| `tags` | `ix_tags_name_lower` | `[sa.literal_column('lower(name)')]` | no | migration ee9eb74e4b4b |
| `tags` | `ix_tags_parent_id` | `parent_id` | no | migration 31d43b7e01c0 |
| `title_contents` | `uq_parent_asset_once` | `parent_title_id`, `asset_id` | yes | migration 31d43b7e01c0 |
| `title_contents` | `uq_parent_child_title_once` | `parent_title_id`, `child_title_id` | yes | migration 31d43b7e01c0 |
| `title_tags` | `ix_title_tags_tag_id` | `tag_id` | no | migration 31d43b7e01c0 |
| `title_tags` | `ix_title_tags_title_id` | `title_id` | no | migration 31d43b7e01c0 |
| `title_types` | `ix_title_types_code` | `code` | yes | migration 9bdf7126f299 |
| `titles` | `ix_titles_title_type_id` | `title_type_id` | no | migration 9bdf7126f299 |
| `videos` | `ix_videos_master_asset_id` | `master_asset_id` | no | migration 31d43b7e01c0 |

